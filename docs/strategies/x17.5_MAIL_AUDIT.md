# MS17 Phase 5 Strategy — Mail Audit and Hardening

Status: Complete.
Parent milestone: [`docs/milestones/17_PRODUCT_READINESS.md`](../milestones/17_PRODUCT_READINESS.md)
Canonical inputs: [`docs/specs/NOTIFICATIONS.md`](../specs/NOTIFICATIONS.md)

Owner tags: `[Agent]` = critical-path code and doc work to drive directly. `[Subagent]` = safe delegated sidecar work. `[User]` = work the user drives directly.

Six RC-blocking findings surfaced during Phase 4 BQA. Findings 1, 2, 5, and 6 are isolated template/mail class corrections. Findings 3 and 4 share one root cause in the delivery service and are fixed together.

## Root causes

**Findings 1, 2, 5, 6 — Mail template and header defects**

- **Finding 1:** `invoice-paid.blade.php` renders the TXID as plain text with no word-break treatment. Bitcoin TXIDs are 64-character hex strings that overflow their container on narrow viewports. Mail clients require inline styles — class-based rules are stripped.
- **Finding 2:** Both past-due templates label the outstanding USD balance as approximate, but `number_format($outstandingUsd, 2)` produces a to-the-cent figure. The qualifier is false.
- **Finding 5:** All outbound client-facing mail uses `no-reply@cryptozing.app` as the from address and sets no `Reply-To` header. The issuer owns the client relationship — when a client replies to an invoice, receipt, or past-due notice, that reply should reach the issuer, not a dead address. Fix: set `Reply-To` to the issuer's email on all client-facing Mailable classes. The `From` address stays `no-reply@cryptozing.app`.
- **Finding 6:** `invoice-ready.blade.php` renders `{{ strtoupper($invoice->status ?? 'draft') }}` directly to the client. "DRAFT" and "SENT" are internal issuer terms. The client-facing label for an invoice awaiting payment is "Open."

**Findings 7 & 8 — Underpayment and overpayment alert delivery log bloat**

`maybeSendUnderpayAlert()` and `maybeSendOverpayAlert()` use a timestamp-based `shouldSend()` cooldown gate (default 24 h). Once the cooldown expires, `queue()` is called unconditionally — even when a delivery for the same context key already exists. If the queued delivery is never processed (worker down, mail disabled), `queue()` finds an existing `queued` row and writes a new `skipped` row every 24 h. For overpayment alerts, a parallel pattern hits the failed-delivery guard: once a delivery for a TXID context key has failed, each subsequent post-cooldown cron run creates an "already attempted for this trigger" skip. Neither type was addressed by the Phase 5 migration, which targeted only past-due rows. Result: 40,365 skipped underpayment rows and 24,974 skipped overpayment rows — 99.8% of all delivery log entries are noise.

**Finding 9 — Delivery log display shows all rows including skipped**

The delivery log on the invoice show page iterates `$invoice->deliveries` with no filter. All skipped rows render in the table, making the log unreadable on affected invoices.

**Findings 3 & 4 — Past-due delivery log bloat**

`InvoiceAlertService::sendPastDueAlerts()` iterates all three sequence slots on every cron run. Inside each iteration it calls `InvoiceDeliveryService::queue()`, and `queue()` always calls `createDelivery()`, which always writes a new `InvoiceDelivery` row — even when the result is `skipped`. The loop's `break` only fires when a slot produces a newly-queued delivery. Once all three slots have been sent, every subsequent daily cron run completes the full iteration and writes 2–6 skipped rows per invoice (issuer + client × up to 3 slots). An invoice past-due for six months accumulates ~1,080 rows; a year produces thousands. This is also why the delivery log appears to show constant activity — no extra mail is being sent, but the log fills with noise.

## 1. Mail template and header corrections

1. [x] [Agent] In `invoice-ready.blade.php`, replace `{{ strtoupper($invoice->status ?? 'draft') }}` with the literal `Open`.
2. [x] [Agent] In `invoice-past-due-client.blade.php`, remove "approximately" from the outstanding balance line.
3. [x] [Agent] In `invoice-past-due-issuer.blade.php`, remove "(approx.)" from the outstanding balance line.
4. [x] [Agent] Add `replyTo($invoice->user->email, $invoice->user->name)` to the `envelope()` method of every client-facing Mailable class. Client-facing classes are: `InvoiceReadyMail`, `InvoicePastDueClientMail`, `InvoicePaidReceiptMail`, `InvoicePaymentAcknowledgmentClientMail`, `InvoiceUnderpaymentClientMail`, `InvoiceOverpaymentClientMail`, `InvoicePartialWarningClientMail`. Issuer-facing classes receive no reply-to.
5. [x] [Agent] In `invoice-paid.blade.php`, wrap the TXID value in an inline-styled span: `<span style="word-break: break-all; font-family: monospace;">{{ $invoice->txid ?? '—' }}</span>`.

## 2. Delivery log hardening

1. [x] [Agent] Add `deliveryExists(Invoice $invoice, string $type, string $recipient, string $contextKey): bool` to `InvoiceDeliveryService`. Uses `matchingDeliveries()` with `->exists()` — read-only, no rows created. Checks any status.
2. [x] [Agent] In `InvoiceAlertService::sendPastDueAlerts()`, before the queue calls for each slot, check whether a delivery already exists for both the issuer and client using `deliveryExists()`. If both already have a row for that context key, `continue` to the next slot without calling `queue()`. Only call `queue()` when at least one side has no existing row.
3. [x] [Agent] Write and run a one-time cleanup migration that deletes `invoice_deliveries` rows where `type` is `past_due_issuer` or `past_due_client`, `status` is `skipped`, and a corresponding sent row already exists for the same `invoice_id` + `type` + `recipient` + `context_key`. This removes the accumulated noise without touching any row that represents an actual send or a legitimate first-attempt skip. Log the count of deleted rows to the migration output. (Purged 126 rows.)
4. [x] [Subagent] Write feature tests covering: (a) a second cron run against an invoice with all three slots sent creates no new delivery rows; (b) a fresh past-due invoice still produces a queued delivery on first run; (c) slot 2 still fires correctly once the invoice is 7+ days past due and slot 1 is already sent.
5. [x] [Agent] In `InvoiceAlertService`, add a `deliveryExists()` guard in `maybeSendOverpayAlert()` and `maybeSendUnderpayAlert()`: after computing the context key, if the context key is non-null and a delivery already exists for that type + client email + context key, return early without calling `queue()`. This mirrors the past-due slot guard and prevents generating skip rows on every post-cooldown cron cycle.
6. [x] [Agent] Write and run a one-time cleanup migration that deletes `invoice_deliveries` rows where `type` is in `['client_underpay_alert', 'issuer_underpay_alert', 'client_overpay_alert', 'issuer_overpay_alert']`, `status` is `skipped`, and `error_message` is one of `'A matching delivery is already queued.'`, `'A matching delivery has already been attempted for this trigger.'`, or `'A matching delivery has already been sent.'` These are pure idempotency-guard echoes with no informational value. Log the count before deleting.
7. [x] [Agent] In `invoices/show.blade.php`, filter `skipped` rows out of the delivery log table. The entry count in the panel header reflects only non-skipped rows. The outer panel visibility check (`isNotEmpty()`) stays against all deliveries so the panel still appears when a send was attempted even if it was skipped.

## 3. Receipt resend

Issuers have no way to resend a receipt once one has been sent. The existing `queue()` idempotency guard blocks re-sends for `receipt` type once any `sent` row exists for that invoice + recipient + null context key. A resend is an explicit issuer action and should be treated as a distinct intent.

**Design decisions:**
- `InvoiceDeliveryService::queueResend()` owns the resend path. It generates a `resend_{uuid}` context key internally, making each resend intent unique to the idempotency guard without modifying `queue()`.
- Cooldown: before queuing, check for any recent receipt delivery (any context key) within `manual_send_cooldown_minutes`. This prevents spam-clicking. The check is in `queueResend()` so the cooldown is enforced consistently regardless of entry point.
- UI: a "Resend receipt" button on the invoice show page, visible only when a receipt has already been sent (`status` in `['queued', 'sending', 'sent']`).
- After resend: redirect back to the invoice show page with a success or throttled flash message.

1. [x] [Agent] Add `queueResend(Invoice $invoice, string $type, string $recipient): ?InvoiceDelivery` to `InvoiceDeliveryService`. Internally generates context key `'resend_' . Str::uuid()`. Before queuing, checks for any recent delivery of the same type on the same invoice (regardless of context key) within `manualSendCooldownMinutes()` — if found, returns `null` without creating any row. Otherwise delegates to `queue()` with the generated context key and returns the new delivery.
2. [x] [Agent] Add a `resendReceipt` action to `InvoiceDeliveryController` (POST). Gate: invoice must be paid, must belong to the authenticated user, and must have at least one sent receipt delivery. Call `queueResend()` — if `null` is returned, flash a throttle message informing the user when they can resend and redirect back; if a delivery is returned, flash success and redirect back to `invoices.show`.
3. [x] [Agent] Add the POST route for the resend action to `routes/web.php`.
4. [x] [Agent] On the invoice show page, render a "Resend receipt" button in the receipt delivery section. Visible only when the invoice is paid and a receipt has been sent. Button submits the POST route.
5. [x] [Subagent] Write feature tests: (a) resend queues a new receipt delivery with a `resend_` context key; (b) a second resend within the cooldown window returns null and creates no row; (c) resend is blocked when no prior receipt has been sent; (d) resend is blocked when the invoice is not paid.

## 4. Phase 3 correction

1. [x] [Agent] In `resources/views/support/dashboard.blade.php`, move the monitoring panel above the issuer grants table. The spec required it above; it was built below.

## 5. Verify Phase 5

1. [x] [Agent] Run `./vendor/bin/sail artisan test` — full suite passes cleanly. (330 tests, 1623 assertions)
2. [x] [Agent] Pre-BQA smoke checks via Tinker — instantiate each affected mailable and verify rendered output and envelope headers:
   - [x] `InvoiceReadyMail` — status renders `Open`, `Reply-To` set to issuer email.
   - [x] `InvoicePastDueClientMail` — no "approximately" qualifier, `Reply-To` set to issuer email.
   - [x] `InvoicePastDueIssuerMail` — no "(approx.)" qualifier, no `Reply-To` header.
   - [x] `InvoicePaidReceiptMail` — word-break span present on TXID, `Reply-To` set to issuer email.
3. [x] [Agent] Run `invoices:send-past-due-alerts` twice against an invoice with all three slots already sent — confirmed 0 new `invoice_deliveries` rows on the second run.
4. [x] [Agent] Pre-BQA smoke check for resend: via Tinker, call `queueResend()` against a paid invoice with a sent receipt and confirm a new delivery row is created with a `resend_` context key; call it again immediately and confirm the second attempt is skipped due to cooldown.
6. [x] [Agent] Before running the cleanup migration, query `invoice_deliveries` for rows matching the deletion criteria (type in the four alert types, status `skipped`, error message in the three idempotency strings) and record the count. Run the migration. Query again and confirm `before − after` equals the count the migration logged. (Before: 65,747. Migration logged: 65,747 deleted. After: 0. Verified.) Then run `./vendor/bin/sail artisan test` — full suite passes cleanly. (334 tests, 1636 assertions.)
7. [x] [User] Browser QA:
   - [x] 1. Open the delivery log on an invoice with prior underpay/overpay activity — confirm skipped rows are no longer visible in the table.
   - [x] 2. Confirm the support dashboard monitoring panel renders above the issuer table.
8. [x] [User] Browser QA (Phase 5 original):
   - [x] 1. Send a receipt email for a paid invoice and open it on a narrow viewport — confirm the TXID wraps within its container without overflowing.
   - [x] 2. On a paid invoice with a sent receipt, confirm the "Resend receipt" button is visible.
   - [x] 3. Click "Resend receipt" — confirm a success flash appears and a new delivery row exists in the log.
   - [x] 4. Click "Resend receipt" again within the cooldown window — confirm a throttle notice appears and no duplicate delivery is queued.
