# MS16 Phase 2 Strategy - Safeguards + Provider Recovery

Status: Active. Shared safeguards and Laravel Mailgun wiring are now in the app, but stronger two-layer idempotency hardening is still required before any controlled live Mailgun verification send.
Parent milestone: [`docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md`](../milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md)
Depends on: [`docs/strategies/16.1_DELIVERY_BASELINE_AUDIT.md`](16.1_DELIVERY_BASELINE_AUDIT.md)
Canonical inputs: [`docs/specs/NOTIFICATIONS.md`](../specs/NOTIFICATIONS.md), [`docs/PRODUCT_SPEC.md`](../PRODUCT_SPEC.md), [`docs/ops/RC_ROLLOUT_CHECKLIST.md`](../ops/RC_ROLLOUT_CHECKLIST.md)

Phase 2 restores a trustworthy delivery path by combining app-side safeguards with provider recovery and the locked Mailgun HTTP API transport direction.

## 1. Confirm the provider state
1. [x] Determine the actual Mailgun/domain sendability state and capture concrete evidence for throttling, blocking, or reputation damage. If dashboard or provider-console access is required, treat that step as `Guided User` work and capture only the evidence needed to confirm Mailgun HTTP API remains viable for production use.
   1. [x] Inventory the current app-side Mailgun inputs before opening provider tools:
      - `MAIL_MAILER=mailgun`
      - `MAILGUN_DOMAIN=mailer.cryptozing.app`
      - `MAILGUN_ENDPOINT=api.mailgun.net`
      - `MAIL_FROM_ADDRESS=no-reply@cryptozing.app`
      - `MAIL_ALIAS_ENABLED=true`
      - `MAIL_ALIAS_DOMAIN=mailer.cryptozing.app`
      - Mailgun secret is configured in the current app env
      - Current implication: the local environment is now pointed at Mailgun HTTP API, but controlled provider verification is intentionally blocked until the stronger idempotency guard lands.
   2. [x] `Guided User`: inspect the Mailgun account/domain state for the minimum facts we need to decide whether the transport is healthy:
      1. [x] `mailer.cryptozing.app` is verified and no obvious warnings or provider blocks were visible in the current dashboard pass.
      2. [x] Recent Mailgun event data is not available from the current dashboard pass because the account only retains one day of logs and no recent events are visible.
      3. [x] The Mailgun dashboard explicitly shows the domain region as US, so the `api.mailgun.net` endpoint match is settled from the dashboard without needing live-send proof for that part.
   3. [x] Record only the concrete outcomes needed for execution:
      - verified
      - no visible throttling / blocking warning from the current dashboard pass
      - endpoint match confirmed (`US` region with `api.mailgun.net`)
      - provider events missing / insufficient from the current dashboard pass because only one day of retention is available and no recent events are visible
2. [x] Decide whether temporary aliasing is still useful for this phase or whether it now distorts the validation signal too much.
   1. [x] Phase 2 may temporarily disable aliasing for controlled verification sends because, as of `2026-03-28`, the dev-database recipient addresses are under direct user control.
   2. [x] Keep that alias-off exception scoped to this phase’s proof sends only; later milestones must not assume the same recipient-safety condition still holds.

## 2. Implement shared send safeguards
1. [x] Add the shared guardrails Phase 1 proved necessary around delivery creation and dispatch.
2. [x] Prefer one shared intent/dedupe layer over hand-coded per-notice checks that drift by mail class.
3. [x] Ensure manual send, watcher-triggered mail, scheduler-triggered mail, and correction-sensitive queued sends all pass through the same safety policy where appropriate; avoid splitting work across overlapping delivery-creation files until the shared gating seam is chosen.
4. [x] Add or tighten:
   - duplicate-send suppression
   - per-class cooldown enforcement
   - emergency send-disable / circuit-breaker behavior
   - clearer logging and delivery-failure evidence
   1. [x] Parallel: add or expand focused delivery tests without reopening the gating design.
5. [x] Add stronger outbound idempotency before any real provider send.
   1. [x] Make delivery creation idempotent at the queue-intent boundary so concurrent duplicate attempts cannot create multiple real outbound intents.
   2. [x] Make send execution idempotent at the job/provider boundary so retries or duplicate job execution cannot produce a second provider send after the first has already effectively happened.
   3. [x] Base those guards on stable business intent rather than rendered content:
      - invoice id
      - notice class
      - normalized recipient / recipient role
      - payment identity such as `txid` when the notice is payment-scoped
      - never on variable timestamps, provider-added headers, or rendered body drift
   4. [x] Add focused tests for the new idempotency boundary so the first real Mailgun verification send is not the first proof.
      - Current state: the queue-intent boundary has a focused locked-intent test, and the send-boundary path now has focused coverage for claimed `sending` rows and duplicate queued rows that should be skipped instead of resent.

## 3. Ship the Mailgun HTTP API path
1. [x] Treat Mailgun HTTP API as the intended MS16 transport; do not preserve SMTP by default, and keep the notifications spec transport-agnostic while implementation and ops docs carry the concrete transport choice.
2. [x] Implement the standard Laravel Mailgun driver wiring.
   1. [x] Add the Laravel Mailgun transport dependencies (`symfony/mailgun-mailer` and `symfony/http-client`).
   2. [x] Add the Mailgun credentials/config block in `config/services.php`.
   3. [x] Add the `mailgun` mailer definition in `config/mail.php`.
   4. [x] Set and document the intended `MAIL_MAILER=mailgun` path so the HTTP transport is the default send path instead of SMTP.
   5. [x] Update the relevant ops/env docs with the exact Mailgun variables and any region/endpoint requirements.
3. [x] No concrete blocking constraint has appeared so the phase stays on Mailgun HTTP API; if one does appear, document it explicitly instead of quietly drifting back to SMTP.

## 4. Verification and recovery proof
1. [ ] Run controlled send tests against the chosen transport with the new safeguards enabled.
   Real provider verification is now unblocked by `16.2.2.5`, but it should still wait until the remaining verification setup in this section is ready.
   1. [x] Prepare one safe verification path:
      - one owned invoice with a public link enabled
      - one known-safe recipient path
      - outbound mail enabled
      - queue / scheduler path available
      - if aliasing changes for verification, record the exact env flip and rollback note here before the live send so the test remains reversible
      - Completed prep: proof invoice `71` (`MS16-PROOF-20260328`) used `ms14-phase5-c-destination@nospam.site` with `antonina12@nospam.site` cc; aliasing was temporarily flipped off locally for the proof send, then restored immediately afterward.
      - Current caveat: the local `jobs` backlog was already large enough to make a normal worker pass non-isolated, so the proof send executed the exact queued delivery row directly through `DeliverInvoiceMail` after it was created.
   2. [x] Run one real manual send through the Mailgun path and verify the delivery row moves from `queued` to `sent`.
      - Completed proof: delivery `6716` on invoice `71` reached `sent` at `2026-03-28T16:53:15-06:00` with no recorded error message.
   3. [ ] Confirm the corresponding Mailgun event exists and matches the expected domain / endpoint path, closing any endpoint-match proof that was still pending after `16.2.1.2.3`.
      - Current result: the Mailgun events API returned an empty `items` array for both the domain-wide query and the recipient-filtered query immediately after the proof send, so provider-side event evidence is still missing / insufficient rather than disproven.
   4. [ ] Confirm the received message matches the intended verification setup, including the current aliasing decision.
      - Pending `Guided User` confirmation from the controlled `nospam.site` inboxes used for invoice `71`.
   5. [x] Immediately attempt a second manual send and verify cooldown or duplicate suppression records `skipped` instead of issuing a second provider send.
      - Completed proof: delivery `6717` on invoice `71` recorded `skipped` with `Invoice email skipped because the same notice was already queued or sent recently.`
   6. [x] Temporarily set `MAIL_OUTBOUND_ENABLED=false`, attempt another send, and verify the app records `skipped` without producing a provider-side send.
      - Completed proof: delivery `6718` on invoice `72` recorded `skipped` with `Outbound mail is temporarily disabled.`
   7. [x] Queue a send, change invoice truth before delivery (for example disable the public share link or change the recipient context), and verify the queued attempt becomes `skipped` truthfully.
      - Completed proof: delivery `6719` on invoice `73` recorded `skipped` with `Public share link disabled before send.`
   8. [ ] Capture the exact evidence needed to close Phase 2:
      - app-side delivery row / status
      - provider-side event proof
      - received-message proof where applicable
   9. [ ] Update the RC rollout notes with any provider/aliasing findings that are now settled enough to document.
2. [x] Confirm that duplicate-send prevention and send-disable behavior work without breaking legitimate delivery attempts.
3. [x] Verify that queued deliveries still mark `skipped` or `failed` truthfully when invoice state changes or transport errors occur.

## 5. Verify Phase 2
1. [ ] Mailgun sendability is restored or an explicit alternate path is chosen.
   Close this only after the provider-state evidence, `16.2.2.5`, and at least one controlled Mailgun verification send all succeed, or after a concrete fallback is explicitly documented.
2. [x] Shared outbound-mail safeguards are implemented and documented.
   The stronger two-layer idempotency guard in `16.2.2.5` is now implemented and documented; keep later verification focused on provider proof rather than reopening the safeguard design.
3. [x] Mailgun HTTP API is explicit as the chosen transport and reflected in docs/code, or any blocking fallback is explicitly justified.
