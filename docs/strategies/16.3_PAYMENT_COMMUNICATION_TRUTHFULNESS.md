# MS16 Phase 3 Strategy - Payment Communication Truthfulness

Status: Active.
Parent milestone: [`docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md`](../milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md)
Depends on: [`docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md`](16.2_SAFEGUARDS_PROVIDER_RECOVERY.md)
Canonical inputs: [`docs/specs/NOTIFICATIONS.md`](../specs/NOTIFICATIONS.md), [`docs/specs/PAYMENT_CORRECTIONS.md`](../specs/PAYMENT_CORRECTIONS.md), [`docs/PRODUCT_SPEC.md`](../PRODUCT_SPEC.md)

Phase 3 aligns payment-triggered communication with the now-trustworthy delivery path and resolves the remaining truthfulness and alert-surface questions.

## 1. Lock the acknowledgment versus receipt model
Do not split the core acknowledgment-versus-receipt decision across sidecars; it drives too many downstream surfaces.

1. [x] MS16 introduces an automatic low-information payment acknowledgment before any reviewed receipt. Keep the copy deliberately narrow and non-promissory. Approved direction:
   - `A Bitcoin payment of 0.00123456 BTC was detected.`
   - `No action is needed right now.`
   - `The invoice issuer has been notified to review it promptly.`
   - Do not ship the final line without the matching owner-side follow-through: a prompt, visible review/receipt call to action in the dashboard and invoice payment history.
   - Ambiguity does not block this low-information acknowledgment as long as the copy remains narrow and non-promissory.

## 2. Resolve later-payment ambiguity handling
1. [x] Keep owner validation as a narrow later-payment safeguard for ambiguous higher-certainty payment-triggered communication; do not expand it into a broad rule for all ambiguous payment-triggered mail. Keep the sequencing explicit that this is MS16 follow-up work, not retroactive MS14 reattribution scope.
2. [x] Explicitly decide which outbound classes it covers and which remain outside that gate:
   1. [x] Automatic reviewed receipt / paid confirmation is blocked by ambiguity until issuer review, but issuer sending after review remains allowed.
   2. [x] Low-information acknowledgment is not blocked as long as the copy stays narrow and non-promissory.
   3. [x] Manual sends are not blocked by this ambiguity gate.
   4. [x] Past-due reminders are not blocked by this ambiguity gate.

## 3. Rationalize the live alert surface against the canonical spec
1. [x] Retire repeated partial-payment warnings as a separate alert family and fold them into underpayment behavior.
   - The current repeated warnings come from later partial-payment detections while an invoice still remains underpaid and the cooldown allows another warning cycle.
   - Phase 3 should remove that separate client-facing warning family rather than preserving a repeated partial-payment-specific mail path.
2. [x] Use explicit paired owner/client delivery classes for client-facing notification emails in RC rather than a generic issuer-copy toggle or hybrid split.
3. [x] Decide how delivery-history labels and rows should represent the final notice classes and outcomes. Approved direction:
   - Use `Notice`, not `Type`, in the invoice delivery log.
   - Show concise labels instead of raw storage keys.
   - Keep paired owner/client audience markers explicit in the label, for example `Past-due reminder (client)` and `Underpayment alert (owner)`.
   - Current approved delivery-log labels:
     - `send` -> `Invoice email`
     - `receipt` -> `Receipt (client)`
     - `owner_paid_notice` -> `Paid notice (owner)`
     - `past_due_client` / `past_due_owner` -> `Past-due reminder (client|owner)`
     - `client_overpay_alert` / `owner_overpay_alert` -> `Overpayment alert (client|owner)`
     - `client_underpay_alert` / `owner_underpay_alert` -> `Underpayment alert (client|owner)`
     - `client_partial_warning` / `owner_partial_warning` -> `Partial payment warning (client|owner)` while those legacy rows still exist
   - Future payment-triggered follow-up should keep the acknowledgment-versus-receipt split visible in delivery history once the new rows ship, using labels such as `Payment acknowledgment (client)` for the narrow automatic notice and `Receipt (client)` for the higher-certainty follow-up.
   - Outcome labels should display as `Queued`, `Sending`, `Sent`, `Skipped`, and `Failed`.
   1. [x] Parallel: clean up delivery-history labels and copy once the final notice-class map is decided.

## 4. Ship the supporting UX and settings
1. [ ] Add the owner-facing review/send surfaces required by the final payment communication model.
   - Phase 3 now includes a manual client-receipt send path from the paid invoice payment history plus a dashboard `Review receipt` prompt when a paid invoice still lacks a queued/sent client receipt.
   - Automatic reviewed receipts are now held for review when the paid invoice has multiple active on-chain payments or ignore/reattribution state in its payment history, and that hold still leaves the owner paid notice plus manual review/send path intact.
   - The remaining gap before this item closes is the future acknowledgment/reviewed-receipt split, not the basic owner review/send or automatic reviewed-receipt hold.
2. [x] Refrain from adding an issuer-copy toggle in Phase 3; paired owner/client delivery classes are the approved RC direction unless a later milestone reopens that choice.
3. [ ] Keep the delivery log, invoice surfaces, and any surviving notification settings aligned with the final terminology and outcomes.
   1. [ ] Parallel: finish delivery-log labels and UX follow-through work once the final notice-class map is locked.

## 5. Verify Phase 3
1. [ ] Prepare Browser QA.
   - Agent-run only. Complete this prep before handing the browser pass to a human.
   - Run this only after the Phase 3 implementation items in sections 3 and 4 exist in the product.
   1. [ ] Start the local stack with `./vendor/bin/sail up -d` and leave the `scheduler` service running.
   2. [ ] Prepare one controlled invoice/payment scenario that should auto-send the new low-information acknowledgment without ambiguity.
   3. [ ] Prepare one controlled invoice/payment scenario where ambiguity should block the automatic reviewed receipt / paid confirmation until issuer review.
   4. [ ] Prepare one controlled invoice/payment scenario that still allows the issuer to review and send the higher-certainty follow-up manually after the ambiguity gate holds the automatic path.
   5. [ ] Prepare one controlled scenario that exercises the final paired owner/client delivery classes and the delivery-log labels for those notice rows.
   6. [ ] Prepare one controlled underpayment scenario that proves repeated partial-payment warnings no longer survive as a separate alert family.
   7. [ ] Update items 2 through 6 below with the actual invoice IDs, payment rows, expected delivery rows, and any specific review/send controls that should appear once the implementation is in place.
2. [ ] Run Browser QA.
   1. [ ] `Guided User`: verify the automatic low-information acknowledgment.
      1. [ ] Trigger the prepared non-ambiguous payment scenario.
      2. [ ] Verify the client-facing acknowledgment uses the approved narrow copy and does not overclaim invoice settlement, refund, or receipt certainty.
      3. [ ] Verify the paired owner-side row appears in delivery history as expected.
   2. [ ] `Guided User`: verify the ambiguity gate blocks automatic higher-certainty follow-up.
      1. [ ] Trigger the prepared ambiguous later-payment scenario.
      2. [ ] Verify the low-information acknowledgment still behaves according to the approved narrow model if it survives this scenario.
      3. [ ] Verify the automatic reviewed receipt / paid confirmation does not send while ambiguity remains unresolved.
   3. [ ] `Guided User`: verify the issuer review/send path after ambiguity.
      1. [ ] Resolve or review the prepared ambiguous scenario through the owner-facing flow.
      2. [ ] Verify the issuer can still send the higher-certainty follow-up manually after review.
      3. [ ] Verify the delivery history distinguishes the automatic acknowledgment from the later reviewed follow-up.
   4. [ ] `Guided User`: verify paired owner/client notice behavior and delivery-history labels.
      1. [ ] Open the prepared paired-notice scenario.
      2. [ ] Verify the owner/client rows use the final notice-class labels and outcomes in the delivery history.
      3. [ ] Verify the invoice and settings surfaces use the same terminology as the delivery history.
   5. [ ] `Guided User`: verify repeated partial-payment warnings are gone as a separate alert family.
      1. [ ] Trigger the prepared underpayment / repeated-partial scenario.
      2. [ ] Verify the product uses the final underpayment behavior rather than a separate repeated partial-payment warning class.
      3. [ ] Verify no retired repeated-partial warning labels or rows remain visible in the delivery history.
3. [ ] Payment-triggered communication is truthful under ambiguity, ignore, reattribution, and already-funded-invoice scenarios.
4. [ ] Any retained payment-triggered mail classes have explicit, documented reasons to exist.
5. [ ] Owner review/send paths exist wherever the final communication model promises them.
6. [ ] The targeted Browser QA pass completes without reopening the Phase 3 notice model.
