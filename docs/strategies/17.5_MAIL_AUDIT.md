# MS17 Phase 5 Strategy — Mail Audit and Hardening

Status: Active.
Parent milestone: [`docs/milestones/17_PRODUCT_READINESS.md`](../milestones/17_PRODUCT_READINESS.md)
Canonical inputs: [`docs/specs/NOTIFICATIONS.md`](../specs/NOTIFICATIONS.md)

Owner tags: `[Agent]` = critical-path code and doc work to drive directly. `[Subagent]` = safe delegated sidecar work. `[User]` = work the user drives directly.

Six RC-blocking findings surfaced during Phase 4 BQA. Findings 1, 2, 5, and 6 are isolated template corrections. Findings 3 and 4 share one root cause in the delivery service and are fixed together.

## Root causes

**Findings 1, 2, 5, 6 — Mail template defects**

- **Finding 1:** `invoice-paid.blade.php` renders the TXID as plain text with no word-break treatment. Bitcoin TXIDs are 64-character hex strings that overflow their container on narrow viewports. Mail clients require inline styles — class-based rules are stripped.
- **Finding 2:** Both past-due templates label the outstanding USD balance as approximate, but `number_format($outstandingUsd, 2)` produces a to-the-cent figure. The qualifier is false.
- **Finding 5:** `invoice-past-due-client.blade.php` tells the client "just reply to this email so we can reconcile it" but `MAIL_FROM_ADDRESS` is `no-reply@cryptozing.app`. The app has no incoming mail handling. The copy is a lie and should be removed.
- **Finding 6:** `invoice-ready.blade.php` renders `{{ strtoupper($invoice->status ?? 'draft') }}` directly to the client. "DRAFT" and "SENT" are internal issuer terms. The client-facing label for an invoice awaiting payment is "Open."

**Findings 3 & 4 — Past-due delivery log bloat**

`InvoiceAlertService::sendPastDueAlerts()` iterates all three sequence slots on every cron run. Inside each iteration it calls `InvoiceDeliveryService::queue()`, and `queue()` always calls `createDelivery()`, which always writes a new `InvoiceDelivery` row — even when the result is `skipped`. The loop's `break` only fires when a slot produces a newly-queued delivery. Once all three slots have been sent, every subsequent daily cron run completes the full iteration and writes 2–6 skipped rows per invoice (issuer + client × up to 3 slots). An invoice past-due for six months accumulates ~1,080 rows; a year produces thousands. This is also why the delivery log appears to show constant activity — no extra mail is being sent, but the log fills with noise.

## 1. Mail template corrections

1. [ ] [Agent] In `invoice-ready.blade.php`, replace `{{ strtoupper($invoice->status ?? 'draft') }}` with the literal `Open`.
2. [ ] [Agent] In `invoice-past-due-client.blade.php`, remove "approximately" from the outstanding balance line.
3. [ ] [Agent] In `invoice-past-due-issuer.blade.php`, remove "(approx.)" from the outstanding balance line.
4. [ ] [Agent] In `invoice-past-due-client.blade.php`, remove the sentence "If you already paid, just reply to this email so we can reconcile it." Replace it with direction to use the invoice link instead, e.g. "If you believe this is in error, please review the invoice using the button below."
5. [ ] [Agent] In `invoice-paid.blade.php`, wrap the TXID value in an inline-styled span: `<span style="word-break: break-all; font-family: monospace;">{{ $invoice->txid ?? '—' }}</span>`.

## 2. Past-due delivery log hardening

1. [ ] [Agent] Add `deliveryExists(Invoice $invoice, string $type, string $recipient, string $contextKey): bool` to `InvoiceDeliveryService`. Uses `matchingDeliveries()` with `->exists()` — read-only, no rows created. Checks any status.
2. [ ] [Agent] In `InvoiceAlertService::sendPastDueAlerts()`, before the queue calls for each slot, check whether a delivery already exists for both the issuer and client using `deliveryExists()`. If both already have a row for that context key, `continue` to the next slot without calling `queue()`. Only call `queue()` when at least one side has no existing row.
3. [ ] [Subagent] Write feature tests covering: (a) a second cron run against an invoice with all three slots sent creates no new delivery rows; (b) a fresh past-due invoice still produces a queued delivery on first run; (c) slot 2 still fires correctly once the invoice is 7+ days past due and slot 1 is already sent.

## 3. Verify Phase 5

1. [ ] [Agent] Run `./vendor/bin/sail artisan test` — full suite passes cleanly.
2. [ ] [User] Browser QA:
   - [ ] 1. Send a test invoice delivery email and open it — confirm the status panel shows `Open`, not `DRAFT` or `SENT`.
   - [ ] 2. Trigger a past-due client notice and open it — confirm no "approximately" qualifier on the outstanding balance and no "reply to this email" sentence.
   - [ ] 3. Trigger a past-due issuer notice and open it — confirm no "(approx.)" qualifier on the outstanding balance.
   - [ ] 4. Send a receipt email for a paid invoice and open it — confirm the TXID wraps on a narrow viewport without overflowing.
   - [ ] 5. Run `invoices:send-past-due-alerts` twice against an invoice with all three slots already sent — confirm no new `invoice_deliveries` rows are created on the second run.
