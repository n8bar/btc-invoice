# Task 11 UX Engineering Pass2 (Working Doc)

Status: Finding 2.1 implemented; this doc remains as follow-up traceability for Task 11.

This document is a temporary working strategy. It is not a source of truth like `docs/specs/ONBOARD_SPEC.md`, `docs/milestones/13_UX_OVERHAUL.md`, `docs/PLAN.md`, `docs/PRODUCT_SPEC.md`, or `docs/CHANGELOG.log`, and may be deleted after Task 11 follow-up work ships.

## Purpose
- Capture post-pass findings while current UXInspections continue without scope churn.
- Keep new findings isolated so implementation can be batched intentionally.

## Findings

### Finding 2.1: Non-replay Step 2 should require a deliver-eligible draft invoice
- Observation:
  - In non-replay onboarding, Step 2 previously completed when any invoice existed.
  - Step 3 target selection is draft-only, so users can arrive at Step 3 with no eligible target invoice (for example when a newly created invoice auto-transitions to `paid` due to reused-wallet history).
  - This can delay discovery of wallet-reuse issues and create avoidable confusion.
- Direction:
  - Align non-replay Step 2 gating with deliver targeting: require at least one eligible draft invoice before Step 2 is considered complete.
  - Keep Step 3 completion criteria action-based (public link enabled + delivery attempt), not draft-status-based.
- Additional guard (approved direction):
  - After onboarding invoice creation, if the created invoice is immediately non-draft, show an explicit warning that reused-wallet activity likely triggered early payment detection and instruct the user to create a new draft invoice.
- Desired outcome:
  - Users hit the collision effect earlier (at Step 2) with clearer guidance.
  - Support escalation happens sooner, with less “why is Step 3 blocked?” ambiguity.
- Scope status:
  - Implemented and validated in feature tests + browser QA.

### Implementation strategy for Finding 2.1
- Lock behavior:
  - In non-replay onboarding, Step 2 is complete only when at least one deliver-eligible `draft` invoice exists.
  - Replay keeps the same draft requirement for Step 2 (already aligned).
  - Step 3 completion remains action-based (`public_enabled` + delivery attempt), not draft-status-based.
- Service logic:
  - Update `App\Services\GettingStartedFlow` so non-replay Step 2 uses the same draft-target eligibility rule used by deliver targeting.
  - Update non-replay Step 2 criteria copy to match the draft requirement.
- UX guard behavior on Step 2:
  - If invoices exist but none are draft, show a focused warning callout:
    - “Your latest invoice is no longer draft. Create a new draft invoice to continue.”
  - Include a brief wallet-reuse hint so the likely cause is obvious.
  - Keep a direct CTA to create a new draft invoice.
- Test coverage:
  - Add/adjust feature tests in `tests/Feature/GettingStartedFlowTest.php`:
    - Non-replay user with only paid/sent invoices resolves to Step 2.
    - Step 2 shows the blocked-state warning + create-draft CTA.
    - Creating a new draft invoice allows resolver to advance to Step 3 with target invoice context.
- Browser QA checks:
  - Validate reused-wallet non-replay path fails earlier at Step 2 (not a Step 3 surprise).
  - Validate creating a new draft immediately unblocks progression.
  - Confirm copy is clear and action sequence remains obvious.
