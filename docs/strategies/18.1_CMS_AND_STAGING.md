# MS18 Phase 1 Strategy — CMS Selection & Staging Setup

Status: Active.
Parent milestone doc: [`docs/milestones/18_PRERELEASE_CONTENT_SEO.md`](../milestones/18_PRERELEASE_CONTENT_SEO.md)

**Goal:** Select a CMS and staging workflow as joint decisions, set both up, and confirm the full publish pipeline works end-to-end — so Phase 2 can go straight to writing without tooling uncertainty.

## 1. Define requirements
1. [x] Agree on a short feature set / requirements for both the CMS and staging workflow before listing candidates.

**Must-haves:**
1. Easy content authoring — HTML or Markdown, whichever the tool makes natural
2. GitHub Pages compatible — works with the existing Pages setup for now
3. Shared templating — one layout for consistent nav/header/footer across all pages
4. Per-page SEO metadata — title, description, and canonical controllable per article
5. Portable output — produces static files that work on GitHub Pages now and nginx in production at MS21

**Should-haves:**
6. Staging support — a way to preview content before it goes live; dev server (LAN-only) is the intended staging environment
7. Fast build — small edits and tweaks should not require a slow pipeline
8. Sitemap-friendly — easy to keep `sitemap.xml` current as content is added
9. Video embed support — pave the way for post-RC video content; full video production deferred until after RC1
10. Simple content authoring — add a new article without touching config files

## 2. Evaluate CMS and staging options together
1. [x] Note the MS21 production architecture so candidates can be evaluated with it in mind — accommodate rather than constrain:
   - Dev server runs Laravel via Sail (PHP built-in server in Docker); no nginx in the container. Production will be nginx + PHP-FPM.
   - At MS21 cutover, static content files will be served by nginx directly alongside the Laravel app — path-based split, same domain, no PHP involved for content routes.
   - The staging area built during this phase is intended to become the production content site at cutover — not throwaway infrastructure.
   - Post-RC staging options: new dev path, or a password-protected area on production — to be decided at MS21.
2. [x] List candidate CMS options with their staging implications, evaluated against the requirements above.
3. [x] List candidate staging approaches with their CMS implications.
4. [x] Identify any combinations that are ruled out by the requirements or by each other.

**Evaluation summary:**

| # | Requirement | Jekyll | Hugo | Eleventy | Raw HTML |
|---|---|---|---|---|---|
| 1 | Easy authoring (HTML or Markdown) | ✓ | ✓ | ✓ | △ HTML only |
| 2 | GitHub Pages compatible | ✓ Native | △ Needs Actions | △ Needs Actions | ✓ |
| 3 | Shared templating | ✓ Liquid | ✓ Go templates | ✓ Multiple engines | ✗ Copy-paste |
| 4 | Per-page SEO metadata | ✓ Front matter | ✓ Front matter | ✓ Front matter | △ Manual |
| 5 | Portable static output | ✓ | ✓ | ✓ | ✓ |
| 6 | Staging alongside Sail | △ Requires Ruby (not installed) | △ Requires Hugo binary | ✓ Node already installed | △ Separate file server |
| 7 | Fast build | ✓ At our scale | ✓✓ Best-in-class | ✓ | ✓ No build |
| 8 | Sitemap-friendly | ✓ Plugin | ✓ Built-in | △ Plugin available | △ Manual |
| 9 | Video embed support | ✓ | ✓ Shortcodes | ✓ | ✓ |
| 10 | Content without touching config | ✓ | ✓ | ✓ | △ Tedious at scale |

Raw HTML ruled out (fails Req 3). Jekyll's native GitHub Pages advantage is outweighed by its Ruby dependency — Ruby is not installed on the dev server. Eleventy uses Node which is already present, builds to any output directory, and produces static HTML nginx serves natively. GitHub Pages requires a one-time Actions workflow, which is acceptable given the staging approach no longer relies on GitHub Pages for local preview.

**Staging approach:** Eleventy builds output to `public/content/` inside the Laravel app. Sail already serves `public/` on port 80 — static files there are served directly with no PHP involved. Staging is accessible at the dev server's LAN address with no extra ports or containers. At MS21, the same built files copy directly to the nginx web root.

## 3. Make the joint decision
1. [x] Select a CMS: **Eleventy**.
   - Node 20 already installed on the dev server — no new runtime dependencies.
   - Builds to any configured output directory; pointed at `public/content/` for staging via Sail.
   - Produces portable static HTML; nginx serves it natively at MS21.
   - GitHub Pages deployment via a one-time GitHub Actions workflow.
   - At MS21 cutover, evaluate whether to keep Eleventy or migrate — static output means migration is never a rework.
2. [x] Select a staging approach: **Build to `public/content/`, served by Sail on port 80**.
   - No extra ports, no extra containers, no Ruby.
   - Staging content accessible on the LAN at the existing dev server address.
   - Built output excluded from Laravel git tracking via `.gitignore`; source lives in `site/`.
   - At MS21, copy built output directly to the nginx web root — no rebuild needed.
3. [x] Confirm compatibility: Eleventy and the `public/content/` staging approach are fully compatible and reinforce each other.

## 4. CMS setup
_(Steps derived from requirements that need explicit Eleventy setup work.)_

1. [ ] Add `.eleventy.js` config: set input directory (`site/`), output directory (`public/content/`), and configure a content collection so any `.md` file becomes a page without additional config. _(Reqs 5, 10)_
2. [ ] Add `package.json` with Eleventy and `@11ty/eleventy-plugin-sitemap` (or equivalent). _(Reqs 7, 8)_
3. [ ] Create base layout template: shared nav/header/footer, and `<title>`, `<meta name="description">`, and `<link rel="canonical">` populated from front matter with sensible defaults. _(Reqs 3, 4)_
4. [ ] Convert `site/index.html` to use the base layout.
5. [ ] Configure front matter defaults so new pages inherit baseline SEO fields. _(Req 4)_
6. [ ] Add `public/content/` to Laravel's `.gitignore` — built output is not tracked, source in `site/` is. _(Req 5)_
7. [ ] Add GitHub Actions workflow to build Eleventy and deploy to GitHub Pages. _(Req 2)_
8. [ ] Confirm the existing placeholder page still renders correctly after conversion.
9. [ ] Confirm GitHub Pages builds and serves correctly via Actions.

## 5. Staging setup
1. [ ] Run `npm install` and confirm `npx @11ty/eleventy` builds successfully to `public/content/`. _(Req 6)_
2. [ ] Confirm built content is accessible via Sail at the dev server's LAN address.
3. [ ] Create a test content page and run it through the full staging workflow.
4. [ ] Confirm the test page is reviewable via Sail before it is pushed to GitHub Pages.
5. [ ] Confirm the test page appears correctly on GitHub Pages after push.
6. [ ] Remove or archive the test page after verification.

## Exit Criteria
- [ ] CMS and staging approach selected and documented.
- [ ] Eleventy set up; existing placeholder page unaffected.
- [ ] GitHub Actions workflow deploying to GitHub Pages.
- [ ] Staging workflow confirmed: build to `public/content/`, accessible via Sail.
- [ ] Phase 2 can begin writing content immediately without tooling work.
