CryptoZing changelog
Chronological order: oldest first
Timezone for dates: America/Denver


2025-11-07 | Documented rate precision
`docs/RATES.md` defines USD/BTC rounding and cache TTL rules.

2025-11-07 | Added test hardening suites
Public share SEO, rate refresh caching, BIP21 output, and trash/restore flows covered by Sail tests.

2025-11-07 | PLAN.md established
Plan maintained by Codex; README links to doc.

2025-11-07 | Docker daemon access adjusted
AL9 configured for Sail via socket group.

2025-11-07 | PR #13 merged
Friendly 403 copy standardized to satisfy Authorization tests.

2025-11-08 | Wallet onboarding & derived addresses
`/wallet/settings`, Node-based derivation, and legacy backfill command landed (codex/phase-a-wallet).

2025-11-10 | Watcher scheduling automated
Scheduler runs `wallet:watch-payments` every minute with overlap protection + background execution.

2025-11-10 | Blockchain watcher command wired
`wallet:watch-payments` + mempool client integrated into bootstrap; invoices now auto-mark when payments land.

2025-11-14 | Partial payments UI + outstanding summaries
`invoice_payments` table shipped with watcher backfill, USD-first summaries, and outstanding-targeted QR/BIP21 output.

2025-11-15 | Invoice delivery + automated receipts
`invoice_deliveries` log, manual send form, queue job, and `auto_receipt_emails` toggle landed (codex/invoice-delivery).

2025-11-22 | Docs & DX spec drafted
Added `docs/DOCS_DX_SPEC.md`, reordered PLAN to prioritize UX Overhaul before Docs/DX, and linked README.

2025-11-22 | UX Overhaul ordering set
PLAN Item 12 reordered (dashboard → wallet UX → show/edit polish → public/share refresh → onboarding wizard → toggles → templates → settings polish).

2025-11-22 | Print view + timezone fixes
Print view hides back link on public, uses invoice-created date (Denver), formats BTC/rate via shared helpers; changelog column switched to America/Denver; app timezone set to America/Denver.

2025-11-22 | UX Overhaul spec added
Added `docs/UX_OVERHAUL_SPEC.md` and linked PLAN/README for Item 12 scope.

2025-11-22 | Dashboard snapshot added
Dashboard now shows open/past-due counts, outstanding totals, recent payments with per-user cached snapshot.

2025-11-23 | Landing page refreshed
Dark-themed CryptoZing hero with larger logos, updated copy, staggered feature cards, and CTA/brand badge polish.

2025-11-23 | PLAN alignment updates
PLAN UX Overhaul section synced with spec and mailer/alerts audit now links to `docs/NOTIFICATIONS.md`.

2025-11-23 | UX Overhaul spec updated
Marked dashboard snapshot complete; broadened UI polish to invoices/clients and settings/auth; outputs/DoD realigned with current ToDo list.

2025-11-23 | Light/Dark theme toggle
Per-user theme preference (light/dark/system) added with header toggle, class-based dark mode, and tests; PLAN/UX spec updated to mark it done.

2025-11-23 | Wallet settings mainnet-first
Network selector removed; WALLET_NETWORK env sets network, UI shows badge, xpub validation updated; additional wallets follow the configured network.

2025-11-23 | Partial-payment warning documented
NOTIFICATIONS now lists the proactive partial-payment client warning + owner FYI already described in PARTIAL_PAYMENTS; PLAN/README linked new DX docs.

2025-11-23 | RC rollout checklist added
Added `docs/RC_ROLLOUT_CHECKLIST.md`, linked PLAN item 16 verification and README docs section.

2025-11-23 | PLAN mainnet cutover item added
Inserted mainnet cutover prep ahead of RC deploy and clarified Item 12 as testnet-focused; RC checklist remains linked.

2025-12-01 | Payment & address accuracy scope expanded
PLAN Item 12 now covers confirmation-gated payments (`pending` until confirmed), RBF-safe cleanup for dropped unconfirmed txids, and configurable confirmation thresholds (default 1; per-user post-RC).

2025-12-03 | Partial payments spec clarified
Documented USD-as-source with per-payment rate locking, floating outstanding BTC for QR, and status gating on confirmed USD (pending/partial/paid); aligned PAYMENT_CONFIRMATIONS and PLAN notes.

2025-12-04 | Small-balance resolution spec
Documented exact outstanding display (no dust masking) and a manual “Resolve small balance” adjustment (threshold = `max($1, min(1% of expected USD, $50))`) to settle tiny residual USD and mark invoices paid; see PLAN and PARTIAL_PAYMENTS.

2025-12-05 | Mailer & Alerts scope renamed
PLAN Item 14 is now “Mailer & Alerts Polish + Audit” and explicitly includes copy review/refresh for all outbound messages alongside the pipeline/queue audit.

2025-12-06 | MS12 payment/address verification
Verified corrected derivations on invoices 7/8/10 (testnet indices 11/12/13) and ran `wallet:watch-payments --invoice=7,8,10`; watcher processed paid/partial states as expected with no derivation mismatches.

2025-12-06 | MS12 moved to completed milestones
PLAN now lists Payment & Address Accuracy as completed, with verified derivation checks and watcher sanity runs on invoices 7/8/10; outstanding sats clamp documented.

2025-12-06 | Wallet UI testnet badge scope
PLAN and UX spec now clarify mainnet-default UI: env-driven network with no mainnet badge and a dev-only testnet badge/helper shown only when `WALLET_NETWORK` is testnet.

2025-12-06 | Wallet/Xpub UX breakout spec
Added `docs/WALLET_XPUB_UX_SPEC.md` for wallet UX scope; linked UX Overhaul spec + PLAN to the detailed wallet flow/copy/validation guidance.

2025-12-07 | Wallet network helper + validation
Wallet settings now hide network UI on mainnet, show a testnet-only helper, and enforce network-specific xpub prefixes for primary and additional wallets.

2025-12-11 | UX guardrails reference added
Added `docs/UX_GUARDRAILS.md` (Nielsen/WCAG + form/accessibility guardrails) and linked it from AGENTS, PLAN Milestone 13, and README.

2025-12-11 | UX Overhaul ToDo priority updated
Moved theme-toggle accessibility polish ahead of wallet UX and renumbered UX Overhaul ToDos; updated wallet spec references accordingly.

2025-12-11 | Theme toggle accessibility polish shipped
Added accessible labels/pressed-state semantics, visible focus rings, and larger tap targets to the light/dark/system header toggle; aligned with UX guardrails.

2025-12-15 | UX Overhaul: Helpful Notes scoped
Added UX Overhaul ToDo #6 for a public “Helpful Notes” section (starting with xpub safety + why we ask), and renumbered wallet UX ToDo references.

2025-12-16 | Helpful Notes shipped
Added public `/help` with xpub/zpub safety guidance and seeded categories; deep-linked from Wallet Settings, linked from landing, and set up as an indexable SEO surface (canonical + description).

2025-12-16 | Future: Help CMS deferred
Logged a CMS-style, non-dev editable Help Center as a post-MVP item in `docs/FuturePLAN.md`; RC ships `/help` as a static SEO surface for now.

2025-12-17 | Helpful Notes: wallet key guide
Split xpub safety vs. “import your wallet key” steps, added BlueWallet-first instructions + sweeping guidance, and aligned Wallet Settings labels/errors with the supported formats.

2025-12-17 | Wallet network: testnet3/testnet4 aliases
`WALLET_NETWORK` now accepts `testnet3`/`testnet4` so dev envs can stay explicit while derivation + watcher map to the correct networks.

2025-12-20 | Wallet UX improvements shipped
Wallet settings now include inline key guidance and derive validation preview with friendly errors; validation endpoint and tests updated.

2025-12-20 | Login redirect to wallet setup
Logging in without a configured wallet now redirects to `/wallet/settings` so wallet setup happens before invoicing.

2025-12-20 | Additional wallets UI deferred
Hid the Additional wallets section, improved wallet input contrast in dark mode, and logged the deferral in `docs/FuturePLAN.md`.

2025-12-26 | Logout handles expired sessions
Token mismatches on logout now clear auth state and redirect instead of showing a 419; logout links now submit via buttons so GET `/logout` is never triggered.

2026-01-10 | UX Overhaul spec alignment
Clarified UX Overhaul as PLAN Item 13 and allowed client detail to route to edit until a dedicated show view is needed.

2026-02-18 | Future: client balance tracking + credits
Added a post-MVP FuturePLAN item for per-client credit balances and net outstanding totals (open balances less unspent credits), with an auditable credit ledger.

2026-02-19 | Mailer cooldown + explicit follow-up note
PLAN Item 14 now calls for suppressing duplicate sends of the same invoice notice within a configurable threshold unless the sender explicitly selects a distinct follow-up class (for example, “Second notice”); mirrored in FuturePLAN email notes.

2026-02-19 | UX checklist documented in spec
Added the concrete 10-point human-eyes QA checklist for UX Overhaul Task 9 to `docs/UX_OVERHAUL_SPEC.md`, including completed items and remaining manual sweeps (mobile + dark-mode pass).

2026-02-19 | UX checklist dark-mode sweep checked off
Marked the Task 9 human-eyes dark-mode readability sweep as complete in `docs/UX_OVERHAUL_SPEC.md`; mobile sanity sweep remains open.

2026-02-19 | Wallet key storage length hotfix
Added migration `2026_02_19_192045_widen_wallet_xpub_columns` to widen `wallet_settings.bip84_xpub` + `user_wallet_accounts.bip84_xpub` to `TEXT` so encrypted wallet keys are saved without truncation; added regression coverage in `tests/Feature/UserSettingsTest.php` for realistic-length testnet keys on both primary and additional wallet flows.

2026-02-19 | UX Task 9 checked off
Completed the invoices/clients mobile sanity sweep (no horizontal overflow + action wrapping) and applied a universal 4px base `<main>` gutter (`px-1`) across app/public/guest layouts; marked UX Overhaul Task 9 complete in PLAN/spec docs.

2026-02-19 | UX Task 10 strategy documented
Expanded `docs/UX_OVERHAUL_SPEC.md` Task 10 with implementation lock, acceptance checklist, and verification plan; mirrored PLAN Item 13 Task 10 with an explicit pointer to the locked checklist before code implementation.

2026-02-19 | UX Task 9 moved to Completed Tasks
Updated `docs/UX_OVERHAUL_SPEC.md` so “Invoices & Clients UI polish” now lives under Completed Tasks instead of ToDo, matching PLAN status before Task 10 implementation begins.

2026-02-19 | `.cybercreek` made local-only
Added `/.cybercreek/` to `.gitignore`, removed `.cybercreek/Agents.comm` from version control, and updated `AGENTS.md` guidance to keep coordination/recovery files untracked.

2026-02-19 | Task 10 public/share refresh implementation pass
Refactored print/public rendering to a single-template + partial-block structure, added explicit public states (`active` / `disabled_or_expired`), kept public noindex/print behavior, and added coverage in `PublicShareTest` + `InvoicePaymentDisplayTest` for disabled-state safety and public/private section parity. Mobile sanity sweep remains open in the Task 10 checklist.

2026-02-19 | Onboarding spec doc added + linked
Added `docs/ONBOARD_SPEC.md` as the planning source for UX Task 11, linked Task 11 in `docs/UX_OVERHAUL_SPEC.md`, and synced README + PLAN references (no product implementation yet).

2026-02-21 | UX Task 9 split into Completed + ToDo tracking
Updated `docs/UX_OVERHAUL_SPEC.md` to track Task 9 as both Completed and ToDo (with an explicit merge-back rule once carryover polish is done); kept Task 10 in ToDo with completed checklist items preserved, and gated final Task 10 acceptance on Task 9 ToDo completion. Synced PLAN/FuturePLAN wording to match.

2026-02-21 | Contributor walkthrough rename + link sync
Renamed `docs/get-live/ONBOARDING_WALKTHROUGH.md` to `docs/get-live/CONTRIBUTOR_WALKTHROUGH.md` to clarify it is a contributor/dev runbook (not the user onboarding wizard), and updated references in README, PLAN, Docs/DX spec, and wallet UX spec wording.

2026-02-21 | AGENTS multi-agent coordination guardrails
Added a dedicated `Multi-Agent Coordination` section in `AGENTS.md` to treat secondary agents as role-based (not capability-limited), allow normal dirty-worktree collaboration, require path-scoped commits, and define optional checkout/checkin notes via untracked `.cybercreek/Agents.comm`.

2026-02-21 | AGENTS checkout/checkin closeout requirement
Updated `AGENTS.md` multi-agent guidance to explicitly require checking files back in (or marking them released/abandoned) in `.cybercreek/Agents.comm` before ending a session whenever checkout was used.

2026-02-21 | AGENTS “when done” checkout rule clarified
Refined multi-agent checkout behavior to require check-in before any wait state (before user clarification questions, while blocked/waiting, before task switches, and at session end) so idle agents do not hold file checkouts.

2026-02-21 | UX Task 9/10 boundary clarified
Updated `docs/UX_OVERHAUL_SPEC.md` and `docs/PLAN.md` so Task 9 (ToDo) now covers authenticated invoice/client surfaces only, while public/share narrower-screen verification is explicitly tracked under Task 10.

2026-02-21 | UX Task 9 carryover closed and merged
Marked Task 9 follow-up complete and merged the temporary Task 9 ToDo split back into a single completed Task 9 entry in `docs/UX_OVERHAUL_SPEC.md`; updated `docs/PLAN.md` and `docs/FuturePLAN.md` so Task 10 is no longer gated on Task 9 and remains open only for its own public/share narrower-screen checklist item.

2026-02-23 | UX Task 10 completed and moved to Completed Tasks
Marked the Task 10 public/share refresh acceptance checklist complete (including narrower-screen public/share sanity verification), moved Task 10 from ToDo into Completed Tasks in `docs/UX_OVERHAUL_SPEC.md`, and synced `docs/PLAN.md`/`docs/FuturePLAN.md` status wording.

2026-02-23 | Onboarding spec route/URL + wrapper-step behavior drafted
`docs/ONBOARD_SPEC.md` now locks the onboarding route namespace (`/onboarding`, `/onboarding/{step}`, dismiss/reopen), skip-ahead/resume guard rules, and a hybrid implementation model (lightweight onboarding step shells that hand off to existing wallet/invoice pages instead of duplicating forms).

2026-02-23 | Onboarding spec route namespace renamed to getting-started
Updated `docs/ONBOARD_SPEC.md` to lock the canonical documented route paths and route names to `getting-started` (`/getting-started`, `/getting-started/{step}`, `getting-started.*`) and removed `wizard` wording from the route-shape section prose.

2026-02-23 | Onboarding spec state storage simplified for getting-started
`docs/ONBOARD_SPEC.md` now defines a minimal v1 flow-state model: derived step progress plus only `getting_started_completed_at` and `getting_started_dismissed` on `users`, and simplifies deliver-step resume to `?invoice={id}` or latest user invoice (no persisted last-invoice pointer).

2026-02-23 | Onboarding spec state wording simplified to “two ways to be done”
Rewrote the getting-started state model section in `docs/ONBOARD_SPEC.md` to frame auto-prompt behavior as two done states (completed or dismissed), added a simple auto-show rule, and removed precedence-heavy wording.

2026-02-23 | Onboarding spec: dismiss now counts as a completion path
Simplified `docs/ONBOARD_SPEC.md` getting-started state semantics so `getting_started_completed_at` is the single "done" marker (set by either completing steps or dismissing), `getting_started_dismissed` records completion mode, and reopen clears both fields.

2026-02-23 | Onboarding spec prompt copy drafted as suggestions (not requirements)
Added a non-binding prompt-copy section to `docs/ONBOARD_SPEC.md` with suggested wording/tone for dismiss confirmation, dismiss success, reopen actions, and completion messages; removed prompt copy from the remaining hard open-item list.

2026-02-23 | Onboarding spec copy section simplified to example strings
Replaced the wordier prompt-copy guidance in `docs/ONBOARD_SPEC.md` with a compact non-binding `Copy Examples` section that lists concrete dismiss/reopen/completion strings plus example step-shell titles/bodies/CTAs for wallet, invoice, and deliver.

2026-02-23 | Onboarding spec dropped empty-state placement as an open item
Removed "exact empty-state placement/mobile behavior" from `docs/ONBOARD_SPEC.md` open items so Task 11 stays implementation-light; the spec still requires clear empty-state CTAs into getting-started without locking placement.

2026-02-23 | Onboarding spec accessibility details drafted (simple-first)
Added a concise accessibility section to `docs/ONBOARD_SPEC.md` covering focus order, keyboard flow, status announcements, and a simple-first interaction rule for the getting-started step shells and linked pages; cleared the last Task 11 open-item entry in the draft metadata list.

2026-02-23 | Onboarding spec metadata/date heading cleanup
Removed the `Draft Metadata` section from `docs/ONBOARD_SPEC.md` and stripped date suffixes from section headings to keep the Task 11 spec cleaner now that the draft decisions are filled in.

2026-02-23 | Onboarding spec heading labels simplified (no parentheticals)
Removed parenthetical qualifiers from `docs/ONBOARD_SPEC.md` headings (including the title, Scope, route/state sections, Copy Examples, and Accessibility Details) to keep Task 11 spec headings shorter and cleaner.

2026-02-23 | Onboarding spec restored hierarchy parentheticals in title/scope
Re-added parentheticals to the `docs/ONBOARD_SPEC.md` title (`MS13 - UX ToDo #11`) and Scope heading (`from UX Overhaul Task 11`) so the spec keeps its hierarchy/context links while other headings remain simplified.

2026-02-24 | UX Task 11 onboarding wizard implemented + promoted to complete
Implemented the getting-started flow (`/getting-started` step shells, dismiss/reopen, derived progress state, contextual resume redirects, and progress strips on wallet/invoice pages), updated login entry to route incomplete users into the wizard, and synced `docs/UX_OVERHAUL_SPEC.md`, `docs/PLAN.md`, and `docs/FuturePLAN.md` to mark Task 11 complete.

2026-02-25 | UX Task 12 scoped as lightweight profile-toggle + polish pass
Refined `docs/UX_OVERHAUL_SPEC.md` Task 12 to keep it implementation-light: place the overpayment note and QR refresh reminder toggles on Profile using the existing toggle pattern, treat settings/auth work as a polish/consistency pass (not a redesign/new auth flow), and keep branding-heavy Invoice Settings cleanup explicitly in Task 14.

2026-02-25 | UX scope split: email templates moved from MS13 Task 13 to MS14
Simplified `docs/UX_OVERHAUL_SPEC.md` by removing the editable email templates item from MS13, renumbering Invoice Settings polish to Task 13, and dropping the Task 14 separation note; synced `docs/PLAN.md` by splitting the combined MS13 toggles/templates checkbox and moving editable email templates into MS14 mailer/audit scope with verification timing called out for during/late MS14.

2026-02-25 | PLAN inserts on-chain attribution hardening milestone after UX Overhaul
Added a new RC milestone in `docs/PLAN.md` immediately after UX Overhaul to address shared account xpub collisions documented in `docs/qa/Finding1.md` (dedicated-account guidance + payment correction tooling), renumbered subsequent milestones (Mailer/Audit -> MS15, Docs/DX -> MS16, Mainnet -> MS17, Deployment -> MS18), and synced `docs/FuturePLAN.md` references/summary text.

2026-02-25 | Clarified dedicated-account guidance wording (receives vs sends)
Tightened the new on-chain attribution hardening milestone and `docs/qa/Finding1.md` to explicitly state that viewing/spending from the account in other wallets is acceptable, but using the same account for additional receives/address generation outside CryptoZing is what breaks invoice attribution.

2026-02-25 | Added product principles doc for funds/property handling
Added `docs/ProductPrinciples.md` to capture product-level principles behind payment attribution, fund access, ownership clarity, uncertainty handling, and reversible correction paths so future UX/engineering decisions can reference a stable baseline.

2026-03-04 | Added post-MVP invoice/client search+filter backlog item
Updated `docs/FuturePLAN.md` to add a new near-term Product UX item for search and filtering on both invoice and client list pages (including query params + pagination-state persistence + coverage expectations), and synced `docs/PLAN.md` to explicitly mark this as deferred post-MVP scope.

2026-03-04 | MS14 phased to front-load wallet key lineage/cursor architecture
Updated `docs/PLAN.md` Milestone 14 into explicit phases, with Phase 14.1 set as the architecture prerequisite (per-key history/cursors, key-aware NDI behavior, invoice key identity), followed by dedicated-wallet UX hardening and correction tooling. Synced `docs/FuturePLAN.md` to keep multi-wallet selection UI post-MVP while pointing core wallet lineage/cursor work to active PLAN scope.

2026-03-04 | UX Task 12 rewritten to match current-state scope
Updated `docs/UX_OVERHAUL_SPEC.md` Task 12/13 to reflect what is already shipped vs. what remains: Task 12 now focuses on Profile communication toggles (overpayment gratuity note + QR refresh reminder, default-on, show/public/print copy gating) plus a lightweight settings/auth consistency pass; Task 13 is now a post-Task12 invoice-settings delta sweep. Synced MS13 wording in `docs/PLAN.md` and scope note in `docs/FuturePLAN.md`.

2026-03-04 | UX Task 12 narrowed to avoid wallet-settings scope creep
Refined `docs/UX_OVERHAUL_SPEC.md` and `docs/PLAN.md` so Task 12 no longer lists Wallet Settings as active polish scope; Task 12 now stays focused on Profile communication toggles plus Profile/Invoice Settings and Login/Logout consistency work.

2026-03-04 | Task 12 profile communication toggles implemented
Added migration `2026_03_04_215126_add_task12_communication_toggles_to_users_table` with per-user default-on flags (`show_overpayment_gratuity_note`, `show_qr_refresh_reminder`), wired Profile save/edit handling, and gated invoice show/public/print client-facing copy accordingly while keeping owner reconciliation guidance visible. Added/updated coverage in `ProfileTest` + `InvoicePaymentDisplayTest`, and verified via `./vendor/bin/sail artisan test` (full suite passing).

2026-03-04 | Task 12 browser QA checklist added to UX spec
Added a dedicated Browser QA sub-item under `docs/UX_OVERHAUL_SPEC.md` Task 12 with the human verification flow for profile toggle persistence and show/public/print copy gating (using the shortened list requested by the team).

2026-03-05 | Task 13 spec now includes draft-only invoice creation
Updated `docs/UX_OVERHAUL_SPEC.md` Task 13 and `docs/PLAN.md` MS13 notes to remove create-time status selection from planned invoice-create UX and require all new invoices to start as `draft` (status can be changed after creation). Synced `docs/FuturePLAN.md` scope summary to confirm this remains active-plan scope with no post-MVP backlog change.

2026-03-05 | Task 13 spec wording updated to cover invoice UX polish
Updated `docs/UX_OVERHAUL_SPEC.md` Task 13 title to remove the parenthetical and rewrote Task13 subitems as a numbered list, clarifying scope now covers invoice settings plus invoice UX finish-up deltas from Task12. Synced `docs/PLAN.md` and `docs/FuturePLAN.md` wording to match.

2026-03-05 | Task 13 subitem numbering adjusted to avoid task-count inflation
Updated `docs/UX_OVERHAUL_SPEC.md` Task 13 subitems from markdown ordered-list syntax to numbered labels (`- 1)` ... `- 5)`) so they stay scoped as subitems instead of being interpreted as additional top-level tasks in some renderers.

2026-03-05 | Task 13 no-op closeout subitem removed
Updated `docs/UX_OVERHAUL_SPEC.md` Task 13 to remove the prior “close as verified/no-op” subitem since substantive Task13 deltas are already known.

2026-03-05 | Task 13 now includes required client email + DB hardening
Updated `docs/UX_OVERHAUL_SPEC.md` and `docs/PLAN.md` Task 13 scope to require client email on client create/edit flows and enforce `clients.email` as non-null at the schema layer with safe backfill handling for any null legacy rows. Synced `docs/FuturePLAN.md` summary to reflect this remains active-plan scope.

2026-03-05 | Task 13 scope expanded for paid print watermark + branded client-facing payment copy
Updated `docs/UX_OVERHAUL_SPEC.md` and `docs/PLAN.md` Task 13 scope to include (a) a prominent diagonal `PAID` watermark for paid owner/public print views and (b) replacing generic “invoice sender” client-facing over/underpayment wording with biller/brand identity (billing name with fallback). Synced `docs/FuturePLAN.md` latest scope summary.

2026-03-05 | Task 13 scope expanded to hide payment QR/BIP21 on paid invoices across owner + client surfaces
Updated `docs/UX_OVERHAUL_SPEC.md` and `docs/PLAN.md` Task 13 scope to remove payment-action surfaces on paid invoices (hide QR + BIP21/copy actions on owner show, owner print, and active public print) to reduce accidental rescans on older paid invoices. Synced `docs/FuturePLAN.md` latest scope summary.

2026-03-05 | UX Task 12 moved to Completed status
Updated `docs/UX_OVERHAUL_SPEC.md` to move Task 12 from ToDo into Completed Tasks after Browser QA closure, and synced `docs/PLAN.md` checklist status to mark Task 12 fully complete. Updated `docs/FuturePLAN.md` latest scope summary so active MS13 scope now points at Task 13.

2026-03-05 | Task 13 subitems cleaned up (removed meta item, renumbered actions)
Updated `docs/UX_OVERHAUL_SPEC.md` Task 13 to remove the non-action “use Task13 for deltas” line and renumber the remaining subitems so the list is purely actionable.

2026-03-05 | Task 13 scope adds Branding & footer reset-to-defaults control (spec-only)
Updated `docs/UX_OVERHAUL_SPEC.md` and `docs/PLAN.md` Task 13 scope to include a “Reset to my custom defaults” affordance near the top of create/edit Branding & footer sections so users can quickly revert per-invoice overrides to Invoice Settings defaults. Synced `docs/FuturePLAN.md` latest scope summary.

2026-03-05 | FuturePLAN update line narrowed to post-MVP scope only
Rewrote `docs/FuturePLAN.md` “Latest scope update” text to remove active MS13/UX-overhaul status and keep this doc focused strictly on deferred post-MVP backlog updates.

2026-03-06 | Task 13 rewritten into pre-BQA -> implementation -> post-BQA phases
Updated `docs/UX_OVERHAUL_SPEC.md` Task 13 to separate baseline Browser QA, implementation scope, and acceptance/regression Browser QA so execution order is explicit. Synced `docs/PLAN.md` Task 13 wording and PLAN last-updated date.

2026-03-06 | Task 13 scope adds TXID edit-field removal from owner UI
Updated `docs/UX_OVERHAUL_SPEC.md` and `docs/PLAN.md` Task 13 implementation scope to hide the editable invoice-level `TXID` field from invoice edit while retaining backend/internal compatibility for legacy/manual/recovery workflows.

2026-03-06 | Task 13 spec cleanup: Phase A/B/C QA ordering + Footer note move scope
Updated `docs/UX_OVERHAUL_SPEC.md` Task 13 so paid-surface watermark/action verification is tracked in Phase C (post-implementation) instead of Phase A baseline, and added a new Phase B implementation item to move the owner invoice Footer note card to immediately before Payment Details. Synced `docs/PLAN.md` Task 13 bullets.

2026-03-06 | Task 13 spec corrected for communication-toggle IA placement
Updated `docs/UX_OVERHAUL_SPEC.md` Task 13 Phase C to include the correction implementation that relocates the overpayment gratuity and QR refresh reminder controls from Profile UI to Invoice Settings UI, then verifies the new placement in Browser QA. Synced `docs/PLAN.md` Task 13 bullets.

2026-03-07 | Task 13 Phase A/B cleanup: priorities moved to baseline and checked off
Updated `docs/UX_OVERHAUL_SPEC.md` Task 13 to move the quality-priorities line out of Phase B implementation into Phase A baseline Browser QA and mark it complete. Renumbered Phase B implementation items accordingly and synced `docs/PLAN.md` wording.

2026-03-07 | PLAN MS13 verbosity reduced; Task detail deferred to UX spec
Simplified `docs/PLAN.md` Milestone 13 tracking so Task 12/13 stay milestone-level and point to `docs/UX_OVERHAUL_SPEC.md` for detailed Task13 phase scope/checklists. Preserved status snapshot and Phase C IA-correction note without duplicating spec-level implementation bullets.

2026-03-07 | PLAN UX-overhaul wording cleanup (no Task-number labels in milestone bullets)
Updated `docs/PLAN.md` Milestone 13 wording to remove direct Task-number references in UX-overhaul bullets, and removed redundant “complete” wording where `[x]` status already conveys completion. Scope/status are unchanged and remain detailed in `docs/UX_OVERHAUL_SPEC.md`.

2026-03-07 | Task 13 Phase B implementation pass shipped (items 1-9)
Implemented draft-only invoice creation (removed create-time status UI + server-side force-draft), required client email in create/edit plus schema hardening migration (`clients.email` non-null with legacy backfill), paid print watermark on owner/public prints, client-facing biller-branded over/underpayment wording updates, paid-invoice payment-action suppression (owner show/owner print/public print QR/BIP21/copy), Branding & footer reset-to-defaults controls on create/edit, and owner-show footer-note repositioning before Payment Details. Added/updated coverage across `UserSettingsTest`, `ClientShowRedirectTest`, `InvoicePaymentDisplayTest`, `PublicShareTest`, `InvoiceShowEditFlowTest`, and `InvoicePaymentSummaryTest`; verified via `./vendor/bin/sail artisan test` (full suite passing).

2026-03-07 | Task 13 phase boundary correction: IA implementation moved to Phase B
Updated `docs/UX_OVERHAUL_SPEC.md` so the communication-toggle IA correction (move gratuity + QR reminder toggles from Profile to Invoice Settings) is listed under Phase B implementation (item 10), and restored Phase C as Browser QA-only. Synced `docs/PLAN.md` status notes to match the revised Phase B/Phase C split.

2026-03-07 | PLAN MS13 cleaned to milestone-only status line (no Task13 phase/subtask detail)
Updated `docs/PLAN.md` UX-overhaul item for “Invoice settings and invoice UX finish-up” to remove Phase A/B/C and item-level status prose; PLAN now points to `docs/UX_OVERHAUL_SPEC.md` as the single source of Task13 phase/checklist detail.

2026-03-07 | Task 13 Phase B checklist updated: items 1-9 checked off
Updated `docs/UX_OVERHAUL_SPEC.md` Task 13 Phase B to mark shipped implementation items 1-9 as complete and leave item 10 (communication-toggle IA correction move to Invoice Settings) open.

2026-03-07 | Task 13 Item 10 implemented: communication toggles moved to Invoice Settings UI
Implemented the IA correction by removing `show_overpayment_gratuity_note` and `show_qr_refresh_reminder` controls from Profile, adding them under `/settings/invoice`, and keeping persistence/default behavior user-level on `users` booleans. Updated validation/controller handling (`InvoiceSettingsRequest`, `InvoiceSettingsController`, `ProfileUpdateRequest`, `ProfileController`), moved UI controls (`resources/views/settings/invoice.blade.php`), and updated/added feature coverage in `ProfileTest` and `UserSettingsTest`. Verified with `./vendor/bin/sail artisan test` (full suite passing).

2026-03-07 | Task 13 scope expanded for unified Settings IA shell + receipt-toggle placement cleanup
Updated `docs/UX_OVERHAUL_SPEC.md` Task 13 to add new Phase B implementation items for a unified Settings tab shell (`Profile`, `Wallet`, `Invoices`, `Notifications`), keep `Show invoice IDs in list` under Profile, move `Auto email paid receipts` to Notifications, and route account/menu settings entry to the unified Settings destination. Added matching Phase C Browser QA checks.

2026-03-07 | Task 13 menu-cleanup item clarified to explicit account-menu target state
Refined `docs/UX_OVERHAUL_SPEC.md` Task 13 Phase B item 14 (and matching Phase C QA check) to explicitly require collapsing `Profile`, `Wallet`, and `Invoice Settings` into one `Settings` menu item, leaving account menu entries as `Settings`, `Getting Started`, and `Logout`.

2026-03-07 | Task 13 remaining Phase B items implemented (11-14): unified Settings tabs, Notifications move, and menu collapse
Implemented a unified Settings IA surface by adding shared tabs (`Profile`, `Wallet`, `Invoices`, `Notifications`) across settings pages, added `/settings/notifications` for `auto_receipt_emails`, removed `auto_receipt_emails` from Profile update UI/validation/controller, kept `show_invoice_ids` under Profile, and collapsed account-menu links to `Settings`, `Getting started`, and `Log Out`. Added route `settings.index` (redirect to Profile tab), updated page-title mapping for settings routes, expanded feature coverage (`ProfileTest`, `UserSettingsTest`, `DashboardSnapshotTest`), and verified with `./vendor/bin/sail artisan test` (198 passing).

2026-03-07 | Settings IA polish: `Account` tab label + nav-consistent settings tab styling
Updated the unified settings tabs to rename `Profile` to `Account`, shifted tab styling from pill buttons to nav-style underline tabs for visual consistency, and updated the profile form heading to “Account Information.” Added/adjusted coverage in `UserSettingsTest` and verified with `./vendor/bin/sail artisan test` (198 passing).

2026-03-07 | Settings tab bar made sticky across settings pages
Updated `resources/views/settings/partials/tabs.blade.php` so the unified Settings tabs stay visible while scrolling (sticky under the top app nav), improving cross-tab navigation during long settings forms. Synced `docs/UX_OVERHAUL_SPEC.md` wording for Task 13 Phase B/C settings-shell behavior.

2026-03-07 | Settings content-width consistency pass (modeled on Account tab)
Updated settings page containers for Wallet, Invoices, and Notifications from `max-w-3xl` to `max-w-7xl` to match Account-tab responsiveness/width usage. Also ran `./vendor/bin/sail npm run build` so current CSS assets are rebuilt, then verified with `./vendor/bin/sail artisan test tests/Feature/ProfileTest.php tests/Feature/UserSettingsTest.php` (45 passing).

2026-03-07 | PLAN adds pre-RC test-suite rationalization pass under Docs & DX
Updated `docs/PLAN.md` Milestone 16 to include a pre-RC cleanup task for auditing the full test suite and removing redundant/low-signal/obsolete tests while preserving meaningful behavioral coverage. Updated the milestone Definition of Done to require this pass before RC closeout.

2026-03-07 | Print action labels updated to include printer emoji
Updated owner invoice show print action link and print-view action-bar print button from `Print` to `🖨️ Print` for action-label consistency with other emoji-prefixed controls. Verified with targeted feature coverage (`InvoicePaymentDisplayTest`, `PublicShareTest`, `InvoiceShowEditFlowTest`) via `./vendor/bin/sail artisan test` (43 passing).

2026-03-07 | Task 13 Phase C Browser QA ownership reset to human checklist flow
Reverted `docs/UX_OVERHAUL_SPEC.md` Phase C items 2-12 back to unchecked so Browser QA closeout remains human-driven, returned `docs/PLAN.md` Task 13 status to in-progress (`[ ]`), and synced `docs/FuturePLAN.md` status note without introducing post-MVP scope changes.

2026-03-07 | Invoice show action row made sticky + top-of-card edit prompt added
Updated `resources/views/invoices/show.blade.php` so the invoice action/nav row (`Back`, status actions, `Edit`, `Delete`, `🖨️ Print`) stays visible while scrolling (`sticky top-16`), and added a new first child inside the main white card prompting users to `edit` invoice details when changes are needed. Added assertions in `tests/Feature/InvoiceShowEditFlowTest.php` and verified with `./vendor/bin/sail artisan test tests/Feature/InvoiceShowEditFlowTest.php tests/Feature/InvoicePaymentDisplayTest.php tests/Feature/PublicShareTest.php` (45 passing).

2026-03-07 | Task 13 Phase C checklist items 2-6 checked off
Updated `docs/UX_OVERHAUL_SPEC.md` to mark Phase C items 2-6 complete while leaving items 7-12 open for remaining human Browser QA validation.

2026-03-07 | Settings page heading (`h2`) now stays fixed during settings-page scroll
Updated the shared app layout (`resources/views/layouts/app.blade.php`) to make the settings-page header bar sticky under the top nav, then adjusted the settings tabs offset in `resources/views/settings/partials/tabs.blade.php` (`top-32`) so tabs remain sticky below the heading without overlap. Updated `UserSettingsTest` assertions and verified with `./vendor/bin/sail artisan test tests/Feature/UserSettingsTest.php tests/Feature/ProfileTest.php` (46 passing).

2026-03-07 | Task 13 Phase C checklist items 7-8 and 10-12 checked off
Updated `docs/UX_OVERHAUL_SPEC.md` to mark Phase C items 7-8 and 10-12 complete, leaving item 9 open for remaining human Browser QA validation.

2026-03-07 | Settings sticky behavior reverted (header + tabs no longer fixed)
Removed the settings-only sticky header behavior from `resources/views/layouts/app.blade.php` and removed sticky positioning from `resources/views/settings/partials/tabs.blade.php` so both the settings heading and tabs scroll normally. Updated `UserSettingsTest` assertions and verified with `./vendor/bin/sail artisan test tests/Feature/UserSettingsTest.php tests/Feature/ProfileTest.php` (46 passing).

2026-03-07 | Settings shell switched to inner-scroll content pane (no sticky heading/tabs)
Refactored settings pages (`/profile`, `/wallet/settings`, `/settings/invoice`, `/settings/notifications`) to use a lower-hierarchy scroll container: tabs stay in the top shell and only the settings content pane scrolls (`data-settings-shell` + `data-settings-shell-scroll`). Updated `resources/views/profile/edit.blade.php`, `resources/views/wallet/settings.blade.php`, `resources/views/settings/invoice.blade.php`, and `resources/views/settings/notifications.blade.php`, plus `UserSettingsTest` assertions. Verified with `./vendor/bin/sail artisan test tests/Feature/UserSettingsTest.php tests/Feature/ProfileTest.php` (46 passing).

2026-03-07 | Removed settings wheel-proxy script and returned to plain shell scrolling behavior
Reverted the custom wheel forwarding logic from `resources/views/settings/partials/tabs.blade.php` and removed the temporary settings-header hook in `resources/views/layouts/app.blade.php`, keeping the settings shell behavior plain (no special JS scroll interception). Verified with `./vendor/bin/sail artisan test tests/Feature/UserSettingsTest.php tests/Feature/ProfileTest.php` (46 passing).

2026-03-07 | Settings pages returned to standard document scroll (inner scroll shell removed)
Removed the non-standard nested scroll container from settings pages by reverting `data-settings-shell` / `overflow-y-auto` wrappers in `resources/views/profile/edit.blade.php`, `resources/views/wallet/settings.blade.php`, `resources/views/settings/invoice.blade.php`, and `resources/views/settings/notifications.blade.php`. Settings now scroll via the normal page/body scroll path. Updated `UserSettingsTest` assertions, ran `./vendor/bin/sail artisan test tests/Feature/UserSettingsTest.php tests/Feature/ProfileTest.php` (46 passing), and rebuilt assets with `./vendor/bin/sail npm run build`.

2026-03-07 | MS13 closeout: Task 13 completed, Phase C item 9 checked, and PLAN milestone moved to Completed
Updated `docs/UX_OVERHAUL_SPEC.md` to mark Task 13 as completed (including Phase C item 9) and align settings-shell wording with shipped behavior (`Account`, `Wallet`, `Invoice`, `Notifications` tabs visible while content scrolls). Updated `docs/PLAN.md` to move Milestone 13 (UX Overhaul) into Completed Milestones and remove it from the active roadmap. Synced `docs/FuturePLAN.md` status line to reflect MS13 completion.

2026-03-10 | Docs authority reorg: PLAN split into ROADMAP + PRODUCT_SPEC; FuturePLAN replaced by BACKLOG
Added `docs/ROADMAP.md` for RC milestone order/status/dependencies, `docs/PRODUCT_SPEC.md` for global product behavior/invariants, and `docs/BACKLOG.md` for post-MVP work. Converted `docs/PLAN.md`, `docs/FuturePLAN.md`, and `docs/ProductPrinciples.md` into compatibility pointers, updated `AGENTS.md` and `README.md` to the new authority model, and clarified domain-boundary ownership across delivery/notifications/payments docs to reduce drift.

2026-03-10 | Domain-spec consolidation: invoice delivery merged into notifications; payment confirmations merged into partial payments
Merged invoice send, receipt, and shared `invoice_deliveries` mechanics into `docs/NOTIFICATIONS.md`, leaving `docs/INVOICE_DELIVERY.md` as a compatibility pointer. Merged confirmation-threshold and RBF-cleanup rules into `docs/PARTIAL_PAYMENTS.md`, leaving `docs/PAYMENT_CONFIRMATIONS.md` as a compatibility pointer. Updated canonical references in roadmap, product spec, README, and clarified backlog item 11 as advanced payment-ledger admin tooling rather than the already-shipped owner adjustment flow.

2026-03-10 | Removed obsolete compatibility-pointer docs after canonical docs reorg landed
Deleted `docs/PLAN.md`, `docs/FuturePLAN.md`, `docs/ProductPrinciples.md`, `docs/INVOICE_DELIVERY.md`, and `docs/PAYMENT_CONFIRMATIONS.md` after the canonical docs graph fully moved to `docs/ROADMAP.md`, `docs/PRODUCT_SPEC.md`, `docs/BACKLOG.md`, `docs/NOTIFICATIONS.md`, and `docs/PARTIAL_PAYMENTS.md`.

2026-03-10 | Docs tree simplified: detailed specs moved under `docs/specs/`, ops under `docs/ops/`, QA archive/tests consolidated under `docs/qa/`
Moved detailed feature specs out of the `docs/` root into `docs/specs/`, renamed `docs/DOCS_DX_SPEC.md` to `docs/ops/DOCS_DX.md`, moved rollout/contributor docs under `docs/ops/`, moved `docs/tests/**` into `docs/qa/tests/**`, and moved `docs/snapshots/**` into `docs/qa/archive/**`. Updated README, AGENTS, roadmap/product-spec links, and active strategy references to match the new structure.

2026-03-13 | Restored PLAN-first workflow and milestone-doc lane
Replaced `docs/ROADMAP.md` with `docs/PLAN.md` as the human-facing execution dashboard, added `docs/milestones/` for checklist-bearing milestone docs, and promoted the former UX-overhaul spec into `docs/milestones/13_UX_OVERHAUL.md`. Updated README, AGENTS, product/ops/QA docs, and active strategy references to use the restored `PLAN -> milestone doc or strategy` workflow. No compatibility-pointer or redirect docs were left behind.

2026-03-13 | MS14 strategy rewritten into phase-ordered execution flow
Moved `Risks and Mitigations` ahead of the execution plan, renamed the execution section from `Proposed Architecture` to `Phases`, promoted historical-data risk mitigation into `Phase 1`, renumbered the remaining work as `Phase 2` / `Phase 3` / `Phase 4` with `1.x` / `2.x` / `3.x` / `4.x` sub-items, kept testing guidance inside the relevant phases instead of a separate test-plan section, and tightened Phase 1 into a direct backup/delete/reseed policy with an explicit invoice/payment scenario matrix, targeted `testnet4` funding fixtures, and explicit local-only testnet key generation under the watch-only private-key boundary. Updated `docs/PLAN.md` so the next-action wording matches the cleaned strategy structure.

2026-03-13 | Product boundary clarified: CryptoZing remains watch-only
Added a product invariant that CryptoZing must never require, collect, store, process, or depend on private keys or seed phrases for normal operation, and added AGENTS guidance that any local testnet funding keys must stay untracked and outside the product boundary.

2026-03-13 | MS14 Phase 2 strategy aligned to a single cursor ledger
Reworked `docs/strategies/MS14_IMPLEMENTATION_STRATEGY.md` Phase 2 into action-headed execution steps, removed the compatibility-mirror/backfill plan, made `wallet_key_cursors` the only derivation-state source, explicitly called for dropping `wallet_settings.next_derivation_index` and `user_wallet_accounts.next_derivation_index`, and added explicit command-path plus Human/Browser QA coverage for the single-source lineage model.

2026-03-13 | Unsupported wallet-reuse handling documented for MS14
Added product-level rules for unsupported wallet configurations and unsupported invoice state, expanded the wallet UX spec with gentle-but-prominent unsupported-state warning requirements, rewrote MS14 strategy Phase 3 around proactive plus evidence-triggered unsupported-state detection/flagging, renumbered later MS14 phases, and updated `docs/PLAN.md` milestone 14 intent to reflect unsupported wallet reuse detection.

2026-03-14 | MS14 help-content and companion-wallet follow-up documented
Added a Helpful Notes explainer requirement to the wallet UX spec and MS14 Phase 4 so less technical users get plain-language guidance on watch-only limits, dedicated receiving keys, unsupported configuration, and the recommended separate receive/spend pattern. Added a post-MVP backlog item for a separate spending-only companion wallet ecosystem idea.

2026-03-14 | Canonical wallet and onboarding docs synced to MS14 decisions
Updated `docs/specs/WALLET_XPUB_UX_SPEC.md` so it no longer reads as fully done while active MS14 wallet-risk requirements remain upcoming, updated `docs/specs/ONBOARD_SPEC.md` to reflect dedicated receiving-account guidance plus Helpful Notes linkage, clarified in `docs/PRODUCT_SPEC.md` that Helpful Notes is only one reinforcing surface for the dedicated-account rule, and tightened the MS14 strategy header/risk wording to point at the current canonical sources.

2026-03-14 | MS14 Phase 2 advanced from implementation to remaining verification
Updated `docs/PLAN.md` and `docs/strategies/MS14_IMPLEMENTATION_STRATEGY.md` so the current MS14 next action now points at the remaining Phase 2 collision check and Browser QA after the cursor-ledger schema/runtime work, automated coverage, and command sanity runs landed.

2026-03-18 | CryptoZing.app placeholder fenced into `site/` with Pages workflow
Moved the temporary GitHub Pages placeholder out of the repo root into `site/`, added a dedicated GitHub Actions Pages workflow to deploy that static folder, removed root-level placeholder hosting files from the app surface, documented the placeholder boundary in `README.md` and `AGENTS.md`, kept local review available at `/site/index.html` via a tracked `public/site` alias, refreshed the placeholder page to reuse the app landing page logo/color direction with concise mission/product copy plus a GitHub link, and added static SEO assets/metadata (`canonical`, OG/Twitter tags, JSON-LD, `robots.txt`, `sitemap.xml`, and a dedicated preview image) without changing the app runtime.

2026-03-18 | Backlog now captures future quotes model as proposal-to-billing flow
Added a post-MVP backlog item for quotes and quote-to-invoice conversion, documenting the business boundary that quotes are proposal documents while invoices are billing documents, the current direction to keep quote status separate from invoice status, the planned quote lifecycle (`draft`, `sent`, `accepted`, `declined`, `expired`), the future invoice-start state `unsent`, and the decision to defer same-table vs separate-table storage to later spec work.

2026-03-18 | Pages workflow opts into Node 24 ahead of GitHub's June runner switch
Added `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` to the placeholder-site Pages workflow so we exercise the GitHub Actions Pages deployment on Node 24 now instead of discovering compatibility issues when GitHub flips the default runtime in June 2026.

2026-03-18 | MS14 Phase 2 collision check completed; remaining work narrowed to Browser QA
Rebased PR `#41` (`MS14 Phase 2`) onto current `main`, reran `./vendor/bin/sail artisan test` successfully on the rebased branch, completed the shared-account collision verification by temporarily mismatching the current wallet row and confirming `wallet:watch-payments --invoice=57` still followed the invoice's stored lineage, and updated `docs/PLAN.md` / `docs/strategies/MS14_IMPLEMENTATION_STRATEGY.md` so the remaining Phase 2 work is now only the two Browser QA flows.

2026-03-18 | MS14 Phase 2 Browser QA completed
Finished the remaining PR `#41` Browser QA flows by proving a fresh wallet key can save and create invoices after the wallet-row cursor removals, then switching `Tester1` between two keys and confirming the original key resumed at derivation index `2` / cursor `3` instead of reusing an older address.

2026-03-18 | MS14 Phase 3.1 unsupported-state persistence implemented
Added wallet-level unsupported-state fields (`active`, `source`, `reason`, `details`, `flagged_at`) plus matching invoice-level unsupported-state snapshot fields, wired invoice creation to inherit the wallet flag when present, cleared wallet-level unsupported state when the owner replaces the primary wallet key, added feature coverage for both behaviors, and updated PLAN/strategy docs so Phase 3 now advances into proactive detection (`3.2`).

2026-03-18 | MS14 Phase 3.2 proactive unsupported-wallet detection implemented
Added a dedicated proactive detector that scans a bounded window of derived external receive addresses through the existing mempool client when the primary wallet is saved, flags the wallet when an unknown derived address shows prior incoming funds, ignores receive history already explained by this same key's known invoices inside CryptoZing, leaves spend-only history unflagged, and keeps wallet save non-blocking. Added feature coverage for the proactive flag, known-invoice false-positive guard, and spend-only non-trigger case; advanced PLAN/strategy docs to the next Phase 3 step (`3.3`).

2026-03-18 | MS14 Phase 3.3 evidence-triggered unsupported-state detection implemented
Added payment-collision evidence handling to invoice payment sync so a paid shared address now marks the implicated invoices unsupported, flags the current wallet only when the invoice lineage still matches the owner's active primary key, leaves unrelated older invoices untouched, and adds watcher coverage for both the shared-address flagging path and the old-lineage/current-wallet guard. Advanced PLAN/strategy docs to the next Phase 3 step (`3.4`).

2026-03-18 | MS14 Phase 3.4 unsupported-state warning surfaces implemented
Added app-chrome unsupported indicators (user-menu label, Settings dot, Wallet-tab dot) that appear only while the wallet is flagged, added gentle corrective wallet-warning copy on `/wallet/settings`, surfaced unsupported invoice state in the invoice list and owner show page so flagged invoices stay visibly unsafe after wallet replacement, added feature coverage for both the wallet warning surfaces and invoice unsupported markers, and synced the wallet UX spec plus PLAN/strategy docs to Phase 3 verification (`3.5`).

2026-03-18 | MS14 Phase 3 Browser QA corrected unsupported-state status
Browser QA confirmed the gentle wallet warning copy, unsupported invoice snapshotting, and non-retroactive older-invoice behavior, but found the repair-path UI was only partially shipped: the user-menu unsupported label needed a stronger actionable affordance, the intended Settings/Wallet red dots did not visibly surface in the browser, and the invoice create screen still lacked its own flagged-state warning/confirmation. Updated the strategy, wallet UX spec, PLAN, and QA notes to reflect those gaps, and made the user-menu unsupported pill part of the dropdown trigger with stronger styling.

2026-03-18 | MS14 Phase 3 repair path completed after Browser QA follow-up
Recompiled the local Vite assets, strengthened the user-menu unsupported pill, moved the Settings and Wallet red dots before their labels, and reran Browser QA to confirm the unsupported repair path now works end-to-end from the user-menu pill through Settings, Wallet, and the wallet warning block. The invoice create screen still lacks its own flagged-state warning/confirmation and remains follow-up work.

2026-03-19 | PLAN now assigns password-policy hardening to MS16 and flips completed milestone order
Updated `docs/PLAN.md` so MS16 explicitly includes auth/password policy hardening alongside docs/test cleanup, and reversed the Completed Milestones table from descending milestone order (`13 -> 1`) to ascending order (`1 -> 13`).

2026-03-19 | MS14 Phase 3 invoice-create warning follow-up implemented
`/invoices/create` now shows an inline unsupported-state warning when the wallet is flagged, changes the primary CTA to `Create Unsupported Invoice`, adds feature coverage for the flagged create view, and closes the remaining Phase 3 follow-up docs/spec notes around invoice-create warning behavior.

2026-03-19 | MS14 Phase 3 verification completed
Checked off the remaining Phase 3 automated verification items after `./vendor/bin/sail artisan test` passed with 222 tests, and recorded final Browser QA proving the full flagged flow now works end-to-end from wallet save through repair path, flagged invoice creation, and non-retroactive older invoices.

2026-03-19 | Scoped temporary support access before after-this-pr implementation
Added a dedicated support-access spec covering support-only login, owner-granted read-only invoice/client access, fixed-duration expiration, and owner revocation. Updated the product spec, PLAN, and README so the remaining after-this-pr work now has a canonical doc target before code.

2026-03-19 | Temporary support access and overpayment note polish implemented
Added fixed-duration owner-granted support access with a dedicated `/support/login`, support-only read surfaces for invoices/clients, owner grant/revoke controls under Account settings, and config-driven support account allowlisting. Also softened the significant-overpayment owner note so it distinguishes accidental overpayments from intentional tips without sounding overly prescriptive, and synced README/PLAN/AGENTS/spec docs to the new support-access config and copy behavior.

2026-03-19 | AGENTS now requires review of new implementation-shaping docs
Added an AGENTS rule that if a new doc/spec is created and it shapes future implementation scope, it must be reviewed by the user before it is treated as approved implementation direction.

2026-03-19 | Support access spec no longer presumes a dedicated support login
Removed the dedicated `/support/login` requirement from `docs/specs/SUPPORT_ACCESS.md` and neutralized the matching AGENTS environment note so the docs do not pre-approve a separate support-login surface before the UX direction is explicitly reviewed.

2026-03-19 | Support access now uses the standard login surface
Removed the separate `/support/login` implementation so support accounts now authenticate through the standard login page and still land on `/support` after sign-in. Synced the support-access spec to the approved “same login as owners for now” wording and updated auth coverage accordingly.

2026-03-19 | MS14 focus returned to Phase 4 dedicated-wallet UX hardening
Merged the after-this-pr support/copy follow-up and updated `docs/PLAN.md` so the current MS14 next action now points at Phase 4 wallet-settings guidance, onboarding reinforcement, and the Helpful Notes explainer.

2026-03-19 | MS16 now explicitly owns the minimum viable support UI pass
Updated `docs/PLAN.md` milestone 16 from `Docs & DX` to `Docs, DX & Support UX` and tightened its short intent so the RC roadmap clearly assigns the minimum viable support UI and access-flow pass to MS16 alongside the existing docs, auth-hardening, and test-rationalization work without implying a full support console.

2026-03-19 | MS14 Phase 4.1 wallet settings copy now reinforces dedicated receiving accounts
Added a dedicated receiving-account guidance block to `/wallet/settings` that explicitly requires a dedicated account key for invoice receives, warns that reusing the same account for receives elsewhere can attach payments to the wrong invoice, clarifies that viewing/spending elsewhere is fine, adds focused feature coverage for that copy, and advances `docs/PLAN.md` / `docs/strategies/MS14_IMPLEMENTATION_STRATEGY.md` toward Phase 4 onboarding and Helpful Notes work.

2026-03-19 | MS14 Phase 4 onboarding and Helpful Notes now reinforce dedicated receiving accounts
Updated the getting-started wallet step with a concise dedicated-account warning block and direct Helpful Notes link, added a dedicated receiving-account article to the public `Helpful Notes` page that explains what breaks tracking, what remains safe, what unsupported configuration means, and how to fix it with a fresh key, added back-link handling for onboarding-driven help visits, and advanced Phase 4 docs so the next action is verification and Browser QA.

2026-03-19 | MS14 Phase 4.4 traceability and automated verification are complete
Linked the wallet-settings dedicated-account guidance block directly to the matching Helpful Notes anchor, tied the wallet key field to the guidance/helper/feedback regions for clearer keyboard and assistive-tech behavior, added a support-safe `wallet.settings.saved_with_dedicated_guidance` log entry that excludes wallet-key material, expanded feature coverage for the wallet/help link and save-log emission, and ran `./vendor/bin/sail artisan test` with `236 passed`. Updated `docs/PLAN.md`, `docs/strategies/MS14_IMPLEMENTATION_STRATEGY.md`, and `docs/specs/WALLET_XPUB_UX_SPEC.md` so Browser QA is now the remaining Phase 4 gate.

2026-03-19 | Drafted the canonical Phase 5 payment-corrections spec
Added `docs/specs/PAYMENT_CORRECTIONS.md` to define owner-only ignore/restore behavior for wrongly attributed on-chain payments, including correction metadata, recalculation rules, watcher survival requirements, public-vs-owner visibility, and audit/logging expectations. Synced `docs/PLAN.md`, `docs/PRODUCT_SPEC.md`, `docs/specs/PARTIAL_PAYMENTS.md`, `docs/strategies/MS14_IMPLEMENTATION_STRATEGY.md`, and `README.md` so the new spec is the primary next doc pending review before implementation.

2026-03-19 | Phase 4 copy guidance now treats external wallet use as normal and keeps wallet-prefix hints env-aware
Updated the onboarding and wallet UX specs so dedicated-account guidance now says the owner's wallet app remains the normal place to view balances and spend/sweep funds because CryptoZing is watch-only. The wallet settings surface should use generic key labels plus one env-aware “usually starts with …” hint (`xpub/zpub` on mainnet, `vpub/tpub` on testnet), while Helpful Notes may keep mainnet-style prefix examples in its explanatory import guidance.

2026-03-19 | Shipped the Phase 4 Browser QA copy follow-up for wallet guidance
Updated wallet settings, onboarding, and Helpful Notes so the watch-only model now explicitly says the owner's wallet app remains the normal place to view balances, sweep funds, and spend, while wallet settings now use generic key labels plus an env-aware “usually starts with …” hint and omit `vpub/tpub` on mainnet surfaces. Added focused mainnet/non-mainnet coverage and reran `./vendor/bin/sail artisan test` with `237 passed`.

2026-03-19 | MS14 Phase 4 Browser QA is complete and Phase 5 is now the active next step
Checked off all three Browser QA items in `docs/strategies/MS14_IMPLEMENTATION_STRATEGY.md` after the approved Phase 4 copy follow-up, added `docs/qa/14.4_BROWSER_QA.md` with the final verification note and disposition, and updated `docs/PLAN.md` so the active next action is Phase 5 correction-tooling implementation against the approved `docs/specs/PAYMENT_CORRECTIONS.md` spec.

2026-03-19 | MS14 Phase 5 correction tooling is implemented and awaiting Browser QA
Added auditable payment-correction metadata on `invoice_payments`, shipped owner-only ignore/restore actions on invoice payment history with inline confirmation and reason capture, excluded ignored rows from invoice/public/print/dashboard settlement math while keeping them visible in owner/support audit views, preserved ignored rows across watcher sync, and skipped queued payment-related deliveries when correction state made them untruthful. Added focused coverage for correction state changes, authorization/logging/visibility rules, dashboard exclusion, watcher persistence, and summary math, then reran `./vendor/bin/sail artisan test` successfully with `246 passed`. Updated `docs/PLAN.md`, `docs/PRODUCT_SPEC.md`, and `docs/strategies/MS14_IMPLEMENTATION_STRATEGY.md` so the remaining MS14 Phase 5 gate is Browser QA.

2026-03-19 | Attribution vs unsupported-wallet docs clarified
`docs/PRODUCT_SPEC.md`, `docs/specs/PAYMENT_CORRECTIONS.md`, and `docs/specs/WALLET_XPUB_UX_SPEC.md` now distinguish stale-address wrong-invoice attribution from unsupported shared-wallet evidence; `docs/specs/NOTIFICATIONS.md` now requires owner validation before second-or-later payment-triggered mail on already-funded invoices.

2026-03-19 | Phase 5 correction scope expanded to include reattribution
`docs/PLAN.md`, `docs/strategies/MS14_IMPLEMENTATION_STRATEGY.md`, and `docs/specs/PAYMENT_CORRECTIONS.md` now keep MS14 Phase 5 open for planned same-owner invoice-to-invoice reattribution, rewrite the Phase 5 verification/Browser QA around ignore/restore/reattribute, and clarify the distinction between detected-payment interpretation tools and separate manual adjustment entries.

2026-03-20 | Reattribution docs now cover dual-history visibility and delete safeguards
`docs/specs/PAYMENT_CORRECTIONS.md`, `docs/strategies/MS14_IMPLEMENTATION_STRATEGY.md`, and `docs/PRODUCT_SPEC.md` now require owner-visible reattribution history on both source and destination invoices, relax the destination-picker wording so current-invoice selection can collapse to no change, and block force delete for invoices participating in active reattributions while directing owners back to the source invoice for manual resolution.

2026-03-20 | Payment-correction docs now require a purge-only hard-delete path with DB backstop
`docs/specs/PAYMENT_CORRECTIONS.md`, `docs/strategies/MS14_IMPLEMENTATION_STRATEGY.md`, and `docs/PRODUCT_SPEC.md` now treat force delete as a purge-only path blocked while bookkeeping history remains, require both app-layer guidance and a persistence-layer backstop for destructive deletes, and allow owner/support correction history to link to related invoices while keeping those links off public/print surfaces.

2026-03-20 | Phase 5 checklist now names the shared delete guard and blocker classes
`docs/strategies/MS14_IMPLEMENTATION_STRATEGY.md` now sharpens the existing Phase 5 delete-safeguard checklist so one implementation bullet explicitly covers choosing the persistence-layer backstop and routing all force-delete paths through a shared preflight guard, while one verification bullet now names the blocker classes that must be covered.

2026-03-20 | Reattribution docs now lock the RC storage model and defer the mail-hold gate to MS15
`docs/specs/PAYMENT_CORRECTIONS.md` now defines an `invoice_payments`-centric RC storage model with immutable source provenance plus one active accounting destination at a time, replaces the blunt delete wording with a blocker-resolution matrix, and limits Phase 5 mail behavior to suppressing untruthful sends after reattribution. `docs/specs/NOTIFICATIONS.md` now marks the broader later-payment owner-validation hold as planned MS15 work, and `docs/strategies/MS14_IMPLEMENTATION_STRATEGY.md` now aligns Phase 5 implementation/verification with that storage model and deferral.

2026-03-23 | Reattribution docs now spell out source vs destination visibility rules
`docs/specs/PAYMENT_CORRECTIONS.md` now makes the public/print rule explicit: reattributed-out payments stay visible only in owner history on the source invoice, never show or count on the source invoice’s public/print surfaces, and show/count only on the destination invoice’s public/print surfaces without source-provenance links or labels. `docs/PRODUCT_SPEC.md` now carries the same source-vs-destination visibility invariant.

2026-03-23 | Doc-role guidance now preserves strategies as the sequencing owner
`AGENTS.md` now defines `docs/milestones/**` as milestone-level execution docs and `docs/strategies/**` as the ordered “do this in this order” checklist docs for active execution, while keeping specs canonical for behavior/scope and requiring one obvious checklist owner whenever both a milestone doc and strategy doc exist.

2026-03-23 | Doc-role guidance now spells out checklist depth by layer
`AGENTS.md` now makes the hierarchy explicit: `docs/PLAN.md` owns milestone-level checkoffs, milestone docs own phase-level checkoffs, and strategy docs own the ordered checklist for one phase. Higher-level docs should roll up lower-level completion with a single checkoff instead of duplicating lower-level checklist items.

2026-03-23 | MS14 now has a milestone doc above the current strategy checklist
Added `docs/milestones/14_PAYMENT_ATTRIBUTION_HARDENING.md` as the phase-level rollup for MS14, updated `docs/PLAN.md` so MS14 now points to that milestone doc as the primary next doc while the current detailed sequencing still lives in `docs/strategies/MS14_IMPLEMENTATION_STRATEGY.md`, moved the active next action from Phase 5 scope review into reattribution implementation after the approved doc pass, and updated the README documentation map to link the new milestone doc.

2026-03-23 | MS14 milestone doc now lets the phase checkbox carry status
`docs/milestones/14_PAYMENT_ATTRIBUTION_HARDENING.md` now avoids restating open/closed status in prose under phase items; the phase checkbox carries the status while the supporting text only describes outcome or remaining work.

2026-03-23 | MS14 milestone doc now omits direct strategy references
`docs/milestones/14_PAYMENT_ATTRIBUTION_HARDENING.md` now stays at the milestone/phase layer without naming the unsplit MS14 strategy doc directly; the strategy references can be added back after the strategy is split into phase-specific docs.

2026-03-23 | MS14 strategy sequencing is now split by phase
Replaced the monolithic `docs/strategies/MS14_IMPLEMENTATION_STRATEGY.md` with phase-specific strategy docs for MS14 Phases 1-5, kept the ordered checklists with their existing completion state in those phase docs, updated `docs/PLAN.md` and `README.md` to point at the active Phase 5 strategy, and removed the old monolith instead of leaving a redirect stub.

2026-03-23 | MS14 strategy split now uses terse phase summaries
Removed repeated strategy-role boilerplate from the new MS14 phase docs and tightened `docs/milestones/14_PAYMENT_ATTRIBUTION_HARDENING.md` so the phase rollup now uses checkbox plus one short sentence per phase instead of multi-line outcome/status prose.

2026-03-23 | Task 11 strategy docs now carry the MS13 prefix
Renamed `docs/strategies/TASK11_GETTING_STARTED_STRATEGY.md`, `docs/strategies/TASK11_UX_ENGINEERING_PASS.md`, and `docs/strategies/TASK11_UX_ENGINEERING_PASS2.md` to `docs/strategies/13.11_*`, and updated README plus intra-doc links to match.

2026-03-23 | MS14 Phase 5 strategy now focuses on route-to-done
Reshaped `docs/strategies/14.5_CORRECTION_TOOLING_SAFEGUARDS.md` so it now centers on completed checked items, the remaining ordered execution sequence, and the closure-proof checklist instead of carrying legacy `5.1`-style subsection framing and spec-like restatement.

2026-03-23 | MS14 Phase 5 substeps now use numbered checklists
Converted the `Remaining Sequence` substeps in `docs/strategies/14.5_CORRECTION_TOOLING_SAFEGUARDS.md` from plain bullets to numbered checkbox items so execution depth stays explicit at both checklist levels.

2026-03-23 | MS14 Phase 5 strategy now promotes parent steps to section headings
Removed checkboxes from the completed recap bullets in `docs/strategies/14.5_CORRECTION_TOOLING_SAFEGUARDS.md`, dropped the `Remaining Sequence` wrapper heading, and promoted each remaining parent step into its own `##` section while keeping numbered checkbox substeps beneath it.

2026-03-23 | MS14 Phase 5 verification section now stays phase-scoped
Renamed the closing section in `docs/strategies/14.5_CORRECTION_TOOLING_SAFEGUARDS.md` from `Closure Proof` to `Verify Phase 5` and tightened the automated/browser bullets so they read as Phase 5 verification checklists instead of a second pass at spec wording.

2026-03-23 | MS14 Phase 5 Browser QA now includes setup and action steps
Expanded the Browser QA portion of `docs/strategies/14.5_CORRECTION_TOOLING_SAFEGUARDS.md` into concrete setup and action checklists that name the local stack prerequisites, seeded-owner login, `testnet4` funding/watcher steps, queue/delivery-log expectations, and the exact ignore/restore/reattribute/force-delete behaviors to verify in the browser.

2026-03-23 | MS14 Phase 5 now separates scenario prep from Browser QA
Split the closing manual verification in `docs/strategies/14.5_CORRECTION_TOOLING_SAFEGUARDS.md` into `Scenario Prep` and `Browser QA` so the browser section now stays browser-only while funding, watcher, and queue-state setup live outside it.

2026-03-23 | MS14 Phase 5 now treats BQA prep as agent-owned setup
Renamed `Scenario Prep` in `docs/strategies/14.5_CORRECTION_TOOLING_SAFEGUARDS.md` to `BQA Prep`, made it explicit that the agent owns the funding/watcher/queue-state setup, and added a handoff checklist so the human Browser QA pass starts from prepared invoice IDs and payment rows instead of terminal work.

2026-03-23 | Phase 5 reattribution and delete safeguards now ship in code
`invoice_payments` now carries the reattribution state fields plus immutable source provenance, owner/support payment history now preserves reattributed source/destination context while public/print/dashboard math follows the active accounting destination, force delete now has blocker guidance plus a foreign-key backstop, and the automated coverage now includes reattribution math, visibility, dashboard ownership, and hard-delete blockers. `docs/specs/PAYMENT_CORRECTIONS.md`, `docs/strategies/14.5_CORRECTION_TOOLING_SAFEGUARDS.md`, `docs/milestones/14_PAYMENT_ATTRIBUTION_HARDENING.md`, and `docs/PLAN.md` now reflect that implementation is done and that the remaining Phase 5 work is BQA prep plus human Browser QA.

2026-03-23 | MS14 Phase 5 strategy now uses one verification section
Removed the duplicate closeout section from `docs/strategies/14.5_CORRECTION_TOOLING_SAFEGUARDS.md` so Phase 5 verification now lives in one place with `Automated`, `BQA Prep`, and `Browser QA` instead of a second blurred Browser-QA/test-pass wrapper above it.

2026-03-23 | MS14 Phase 5 strategy now treats prep and browser work as peer steps
Collapsed the separate automated-coverage build/verify pair in `docs/strategies/14.5_CORRECTION_TOOLING_SAFEGUARDS.md` into one completed item, then promoted `BQA Prep` and `Browser QA` into top-level Phase 5 steps so the remaining manual work now reads as `5.7` prepare and `5.8` run.

2026-03-24 | MS14 Phase 5 BQA prep now carries concrete run details
Checked off the completed `Prepare Browser QA` work in `docs/strategies/14.5_CORRECTION_TOOLING_SAFEGUARDS.md`, changed Phase 5 status to browser-only, and replaced the generic Browser QA placeholders with the actual local scenario invoice IDs, payment rows, delivery-log rows, and public URLs for this run.

2026-03-24 | AGENTS checklist-checkoff exception wording is now explicit
Updated `AGENTS.md` so the docs-push rule now says single-item checklist checkoffs in the same active workstream do not need to be pushed right away, instead of describing them with softer batching language.

2026-03-24 | MS14 Phase 5 Browser QA findings now have a dedicated strategy doc
Checked off the first exercised ignore-flow Browser QA steps in `docs/strategies/14.5_CORRECTION_TOOLING_SAFEGUARDS.md` and added `docs/strategies/14.5_BQA_FINDINGS_AND_FIXES.md` so live Browser QA findings now have one place to capture repros and expected-vs-actual behavior before we turn them into an ordered fix sequence.

2026-03-24 | MS14 Phase 5 findings now avoid duplicate checklist ownership
Removed the checkbox from the finding entry in `docs/strategies/14.5_BQA_FINDINGS_AND_FIXES.md` so findings stay descriptive while the eventual fix sequence remains the only checklist in that doc.

2026-03-24 | MS14 Phase 5 Browser QA now identifies remaining payments by visible cues
Rewrote the unchecked reattribution/public-surface Browser QA items in `docs/strategies/14.5_CORRECTION_TOOLING_SAFEGUARDS.md` to use the visible Scenario C payment timestamps and sats/USD amounts instead of internal payment-row IDs, so the tester can match the correct rows directly in the UI.

2026-03-25 | Delivery log timestamps are now compact and MS15 notes delivery-type label cleanup
The owner invoice delivery log in `resources/views/invoices/show.blade.php` now renders queued/sent timestamps as compact `MM-DD-YY HH:MM` 24-hour values while keeping full timestamps in the tooltip, and `docs/specs/NOTIFICATIONS.md` now notes planned MS15 polish to replace raw underscore-separated delivery type keys with concise owner-facing labels.

2026-03-25 | Manual adjustment reversal is now the canonical append-only undo path
`docs/specs/PARTIAL_PAYMENTS.md` now requires owner-facing manual adjustment reversal instead of in-place edit/delete: an inline two-step `Reverse adjustment` -> `Confirm reverse entry` flow must create an equal-and-opposite manual adjustment row with note `reversal of {txid}` while preserving the original entry, and `docs/PRODUCT_SPEC.md` now summarizes that append-only reversal model at the product level.

2026-03-25 | MS14 Phase 5 docs now point at the Browser QA findings follow-up
`docs/PLAN.md`, `docs/milestones/14_PAYMENT_ATTRIBUTION_HARDENING.md`, `docs/strategies/14.5_CORRECTION_TOOLING_SAFEGUARDS.md`, and `docs/strategies/14.5_BQA_FINDINGS_AND_FIXES.md` now reflect that Browser QA is complete, Phase 5 remains active, and the next follow-up PR work is the findings/fix pass rather than more BQA prep.

2026-03-25 | MS14 Phase 5 follow-up fixes now ship on the findings branch
`resources/views/invoices/show.blade.php`, `app/Http/Controllers/InvoicePaymentCorrectionController.php`, `app/Http/Controllers/InvoicePaymentAdjustmentController.php`, `routes/web.php`, and `tests/Feature/InvoicePaymentCorrectionTest.php` now keep ignore/reattribution validation anchored to the same payment row with focused inline errors, add owner-facing manual adjustment reversal, and make `invoice_payments.sats_received` signed so equal-and-opposite adjustment rows can be stored. `docs/specs/PAYMENT_CORRECTIONS.md`, `docs/PLAN.md`, `docs/milestones/14_PAYMENT_ATTRIBUTION_HARDENING.md`, and `docs/strategies/14.5_BQA_FINDINGS_AND_FIXES.md` now point the remaining Phase 5 work at the targeted Browser QA rerun.

2026-03-26 | MS14 findings now include reattribution undo and AGENTS preserves related user doc edits
`docs/strategies/14.5_BQA_FINDINGS_AND_FIXES.md` now records that reattributed payments still lack a clear inline undo path back to the source invoice, and `AGENTS.md` now says uncommitted user doc edits in the same active workstream should be preserved and carried into the next related commit by default.

2026-03-26 | MS14 findings strategy now gives reattribution undo its own fix block
`docs/strategies/14.5_BQA_FINDINGS_AND_FIXES.md` now inserts a dedicated `2.4` fix sequence for adding a clear reattribution undo path back to the source invoice and shifts the follow-up verification block to `2.5`, including a targeted Browser QA rerun step for the undo flow.

2026-03-26 | Payment correction spec now requires explicit reattribution undo before ignore
`docs/specs/PAYMENT_CORRECTIONS.md` now requires an explicit undo path that returns active accounting credit to the immutable source invoice, forbids `Ignore` while a payment is still actively reattributed, and defines the owner UI, routing, logging, testing, and Browser QA expectations for that undo-first model. `docs/PRODUCT_SPEC.md` now carries the same top-level correction invariant, and `docs/strategies/14.5_BQA_FINDINGS_AND_FIXES.md` now records the matching Browser QA finding and folds the ignore-blocking work into the existing `2.4` reattribution-undo fix sequence.

2026-03-26 | Phase 5 findings branch now ships explicit reattribution undo and blocks ignore during active reattribution
`app/Http/Controllers/InvoicePaymentCorrectionController.php`, `routes/web.php`, and `resources/views/invoices/show.blade.php` now add an explicit owner undo path for active reattributions, route destination-side undo submissions through the canonical source invoice/payment pair, remove the fake return-to-source picker path, and hide/block `Ignore` while a payment is still actively reattributed. `tests/Feature/InvoicePaymentCorrectionTest.php` now covers source/destination undo behavior and the ignore-blocking rule, and `docs/strategies/14.5_BQA_FINDINGS_AND_FIXES.md` now checks off the shipped `2.4` implementation items while leaving the human rerun open.

2026-03-26 | MS14 is complete after the targeted Phase 5 follow-up rerun
`docs/strategies/14.5_BQA_FINDINGS_AND_FIXES.md` now marks the last two targeted Browser QA reruns passed and closes the follow-up strategy, `docs/milestones/14_PAYMENT_ATTRIBUTION_HARDENING.md` now marks Phase 5 and MS14 complete, and `docs/PLAN.md` now moves MS14 into the completed milestones list and leaves the plan in a between-milestones state pending insertion of the next milestone from `main`.

2026-03-26 | PLAN now inserts MS15 for CryptoZing.app SEO bootstrap
`docs/PLAN.md` now makes `MS15 - CryptoZing.app SEO Bootstrap` the active milestone so early discovery/indexing work for the placeholder/landing page can begin before go-live, and renumbers the downstream RC milestones from `16` through `19`.

2026-03-26 | Renumbered milestone references now point mail follow-up work at MS16
`docs/specs/PAYMENT_CORRECTIONS.md`, `docs/specs/NOTIFICATIONS.md`, and `docs/strategies/14.1_BASELINE_RESEED.md` now update their live milestone references so later-payment validation, delivery-log label polish, and deferred outbound-mail cleanup point at `MS16` after the insertion of the new SEO bootstrap milestone at `MS15`.

2026-03-26 | Drafted the MS15 milestone doc for CryptoZing.app SEO bootstrap
Added `docs/milestones/15_CRYPTOZING_APP_SEO_BOOTSTRAP.md` as the milestone-level execution doc for early placeholder/landing-page SEO work, updated `docs/PLAN.md` so MS15 now points at that new milestone doc as the primary next doc, and updated the README documentation map to include the new milestone doc.

2026-03-26 | MS15 milestone doc now explicitly treats prior SEO content passes as baseline
`docs/milestones/15_CRYPTOZING_APP_SEO_BOOTSTRAP.md` now states that SEO-oriented content work has already had a couple passes, so MS15 should refine only what the milestone proves needs adjustment rather than reopening the placeholder for a broad rewrite.

2026-03-26 | MS15 milestone scope now constrains content work to metadata hygiene plus minimal copy fixes
`docs/milestones/15_CRYPTOZING_APP_SEO_BOOTSTRAP.md` no longer frames MS15 as strengthening placeholder content in general, renames the second phase to `Metadata + Search-Signal Hygiene`, and limits that phase to titles, descriptions, headings, canonical/schema signals, and only the minimum on-page copy needed to support the existing landing page.

2026-03-26 | Drafted the MS15 Phase 1 strategy for discovery and indexing baseline
Added `docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md` as the ordered checklist for the first MS15 phase, updated `docs/milestones/15_CRYPTOZING_APP_SEO_BOOTSTRAP.md` so the milestone doc now points at that draft strategy as the current phase strategy, kept `docs/PLAN.md` pointed at the milestone doc, and updated the README documentation map to link the active MS15 Phase 1 strategy.

2026-03-26 | PLAN next-action wording now stays milestone-level
`AGENTS.md` now makes the rule explicit that any `Next action` in `docs/PLAN.md` must stay milestone-level rather than pointing at strategies or phase-level checklist steps, and `docs/PLAN.md` now says to review the MS15 milestone doc and begin MS15 execution.

2026-03-26 | Milestone-doc current focus now stays phase-level without pointing at strategies
`AGENTS.md` now makes the same rule explicit for `docs/milestones/**`: current-focus and next-action lines stay phase-level and may include reviewing the current phase strategy, but milestone docs should not point at strategy docs directly. `docs/milestones/15_CRYPTOZING_APP_SEO_BOOTSTRAP.md` now follows that rule.

2026-03-26 | Checklist-depth rules now talk about lower-level detail, not the word strategy
`AGENTS.md` now frames the boundary in terms of detail depth: `docs/PLAN.md` should not pull in phase-level or strategy-detail steps, and milestone docs may mention reviewing the current phase strategy without pulling strategy-level checklist detail up into the milestone layer.

2026-03-26 | Drafted the remaining MS15 phase strategies before Phase 1 execution
Added `docs/strategies/15.3_METADATA_SEARCH_SIGNAL_HYGIENE.md` and `docs/strategies/15.2_VERIFICATION_MONITORING.md` as the later-phase MS15 strategy docs, with the deployment/cutover continuity note later folded up into the MS19 plan entry instead of staying as its own MS15 phase. Updated the README documentation map to include those new phase strategies.

2026-03-26 | MS15 phase strategy filenames now use the 15.x prefix
Renamed the four MS15 phase strategy docs from the longer `MS15_PHASE*` form to `15.1_...` through `15.4_...`, and updated the README documentation map to use the shorter filenames.

2026-03-26 | Docs filenames now use numeric milestone and phase prefixes consistently
Renamed the remaining milestone, strategy, and QA docs away from the old `MS*`, `PHASE*`, and `TASK*` filename patterns into numeric forms such as `13_...`, `14_...`, `15_...`, `13.11_...`, `14.5_...`, and `14.3_...`. Updated the live repo references plus historical changelog paths to match the new filenames.

2026-03-26 | MS15 no longer treats launch handoff as its own phase
`docs/milestones/15_CRYPTOZING_APP_SEO_BOOTSTRAP.md` now uses three phases instead of four, `docs/strategies/15.2_VERIFICATION_MONITORING.md` now carries the final MS15 verification/monitoring phase after moving up from the old `15.4` slot, and the separate `15.3` continuity-handoff draft was removed. `docs/PLAN.md` now puts the placeholder-replacement continuity note where it belongs near `MS19`, while `docs/strategies/15.3_METADATA_SEARCH_SIGNAL_HYGIENE.md` now treats any continuity-sensitive findings as later rollout constraints instead of a dedicated MS15 handoff phase.

2026-03-26 | MS15 now treats derivative public Helpful Notes content as a conditional Phase 1/2 lever
`docs/milestones/15_CRYPTOZING_APP_SEO_BOOTSTRAP.md`, `docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md`, and `docs/strategies/15.3_METADATA_SEARCH_SIGNAL_HYGIENE.md` now treat the current one-page placeholder plus public Helpful Notes mix as a Phase 1 decision point: if Phase 1 proves it is too thin, the later metadata/search-signal phase may expand derivative public Helpful Notes content under `site/` enough to strengthen SEO while the site still reads as a product placeholder rather than a docs encyclopedia.

2026-03-26 | MS15 Phase 1 now keeps the placeholder sitemap single-URL
`docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md` now records the current `15.1.2` decision: keep `site/sitemap.xml` limited to `/` for now because the repo-controlled GitHub Pages placeholder only exposes that one real crawlable placeholder URL, while public Helpful Notes still lives on the Laravel app surface unless a later MS15 phase deliberately publishes derivative Helpful Notes content under `site/`.

2026-03-26 | MS15 Phase 1 now records the rest of the discovery-signal baseline
`docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md` now records that the live placeholder host resolves the intended canonical, social, JSON-LD, and image URLs correctly; that no extra repo-controlled discovery-signal fixes are needed right now; that `site/sitemap.xml` `lastmod` should be maintained manually when the placeholder's crawl-visible `site/` content changes meaningfully; and that the current root robots/sitemap/canonical setup still makes sense for the later live-app handoff as long as deployment preserves that SEO baseline.

2026-03-26 | MS15 Phase 1 now explicitly covers root-URL submission and controlled inbound links
`docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md` now adds an explicit post-verification step to inspect/request indexing for `https://cryptozing.app/` in the verified webmaster tools, adds a new section for a small set of legitimate inbound links we control, and extends the Phase 1 verification checklist to confirm both the root-URL submission step and those controlled links are actually live.

2026-03-26 | MS15 strategies now assign remaining work by who is better positioned
`docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md`, `docs/strategies/15.3_METADATA_SEARCH_SIGNAL_HYGIENE.md`, and `docs/strategies/15.2_VERIFICATION_MONITORING.md` now tag each remaining unchecked step as `Agent`, `User`, or `Shared` so webmaster-tool/account actions stay with the user while repo/live-host/docs work stays with the agent.

2026-03-27 | MS15 strategies now call out safe subagent sidecars
`docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md`, `docs/strategies/15.3_METADATA_SEARCH_SIGNAL_HYGIENE.md`, and `docs/strategies/15.2_VERIFICATION_MONITORING.md` now distinguish critical-path `Agent` work from `Subagent`-safe sidecars so bounded verification, repo-signal checks, and monitoring-note synthesis can be delegated without improvising around the phase order.

2026-03-27 | Strategy docs should expose subagent-friendly parallel sidecars
`AGENTS.md` now says future strategy docs should still keep the primary critical path explicit but should also call out known safe parallel sidecars or path-scoped tasks so subagent use can follow the strategy intentionally instead of improvising around a single-threaded checklist.

2026-03-27 | MS15 strategies now distinguish guided user work from self-driven user work
`docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md` and `docs/strategies/15.2_VERIFICATION_MONITORING.md` now mark the Google/Bing ownership, sitemap-submission, and indexing-check tasks as `Guided User` rather than plain `User`, and `AGENTS.md` now says `Guided User` should be used when the user has the account access but still needs coaching through unfamiliar tooling.

2026-03-27 | MS15 now records the external-tool ownership decision
`docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md` now checks off `15.1.3.1` and records the decision to use a DNS-verified Google Search Console domain property for `cryptozing.app`, with Bing Webmaster Tools initially inheriting that baseline by importing the verified Google property unless later work proves Bing needs its own separate DNS verification.

2026-03-27 | MS15 now spells out the guided Google Search Console verification flow
`docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md` now gives `15.1.3.2` a concrete guided-user procedure for adding the `cryptozing.app` domain property in Google Search Console, using the DNS TXT verification flow, and leaving the record in place if propagation delays verification.

2026-03-27 | MS15 now spells out the guided sitemap-submission flow
`docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md` now gives `15.1.3.4` a concrete guided-user procedure for submitting `https://cryptozing.app/sitemap.xml` in Google Search Console and clarifies that Bing counts as complete once the submitted sitemap reaches `Success` or an equivalent accepted-processing state.

2026-03-27 | MS15 now records the completed Google and Bing verification baseline
`docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md` now checks off `15.1.3.2`, `15.1.3.3`, and `15.1.3.4`, recording that Google Search Console domain verification succeeded via DNS TXT, Bing Webmaster Tools fell back from the import path to direct CNAME verification, and both tools now show `Success` for the submitted `https://cryptozing.app/sitemap.xml`.

2026-03-27 | MS15 now records the clean Google root-URL inspection result
`docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md` now checks off `15.1.3.5` and records that Google Search Console URL Inspection already reports `https://cryptozing.app/` as on Google, indexed, and served over HTTPS, which is enough to count the root-URL inspection/request step as complete even though Bing's equivalent UI remained flaky.

2026-03-27 | MS15 now records the external-tool stability follow-up
`docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md` now checks off `15.1.3.6` and records the concrete follow-up needed to keep the verified baseline stable: leave the Google TXT and Bing CNAME records in DNS, keep access to the same verified Google/Bing accounts, and treat Bing as durably verified through its own CNAME path rather than through the import shortcut.

2026-03-27 | MS15 now records the initial controlled inbound-link set
The GitHub repository homepage now points at `https://cryptozing.app`, `README.md` now includes a direct website link near the top, and `docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md` now checks off the Phase 1 controlled-link selection/setup steps with those two project-scoped surfaces as the baseline inbound-link set.

2026-03-27 | MS15 now records the live controlled-link baseline after merge
`docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md` now checks off `15.1.4.3`, recording that the GitHub repository homepage and README website link are both live on `main`, with the public LinkedIn post also counted as additional controlled discovery help.

2026-03-27 | MS15 now records the Phase 1 monitoring baseline and matching verification roll-up
`docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md` now checks off `15.1.5.1` through `15.1.5.3` and `15.1.6.1` through `15.1.6.7`, documenting the repo-vs-webmaster-tool verification split, the Google/Bing property and sitemap baseline, the short watch list for crawl/index health, and the matching Phase 1 verification results. The only Phase 1 decision still open is whether the placeholder plus public Helpful Notes mix is too thin for MS15 Phase 2.

2026-03-27 | MS15 Phase 1 is now complete and the later metadata/search-signal phase remains conditional
`docs/strategies/15.1_DISCOVERY_INDEXING_BASELINE.md` now closes Phase 1 with the decision that the current one-page placeholder plus public Helpful Notes mix is good enough for now, `docs/milestones/15_CRYPTOZING_APP_SEO_BOOTSTRAP.md` now rolls Phase 1 up as complete and keeps the verification/monitoring phase next in sequence, and `docs/PLAN.md` now reflects that the later metadata/search-signal phase remains in reserve unless future search-signal evidence proves it is needed.

2026-03-27 | MS15 phase numbering now puts the active closeout work second and the conditional content-tuning work third
`docs/milestones/15_CRYPTOZING_APP_SEO_BOOTSTRAP.md` now orders Phase 2 as Verification + Monitoring and Phase 3 as Metadata + Search-Signal Hygiene, `README.md` now points at [`docs/strategies/15.2_VERIFICATION_MONITORING.md`](strategies/15.2_VERIFICATION_MONITORING.md) and [`docs/strategies/15.3_METADATA_SEARCH_SIGNAL_HYGIENE.md`](strategies/15.3_METADATA_SEARCH_SIGNAL_HYGIENE.md), and the strategy filenames/headings now match that reordered sequence so MS15 no longer looks like it is skipping a numbered phase.

2026-03-27 | MS15 Phase 2 kickoff docs now match the reordered sequence
`docs/milestones/15_CRYPTOZING_APP_SEO_BOOTSTRAP.md` and `docs/PLAN.md` now point the current work at the reordered Phase 2 verification/monitoring sequence, and `docs/strategies/15.2_VERIFICATION_MONITORING.md` now labels its final verification section as Phase 2 instead of the stale old numbering.

2026-03-27 | MS15 Phase 2 drops the redundant ownership-reconfirmation section
`docs/strategies/15.2_VERIFICATION_MONITORING.md` no longer asks for Search Console or Bing ownership reconfirmation after those were already completed in Phase 1; the phase now keeps only real verification, monitoring-baseline, and closeout work.

2026-03-27 | MS15 is now complete
`docs/strategies/15.2_VERIFICATION_MONITORING.md` now closes Phase 2 with live-vs-repo verification evidence, a concrete monitoring baseline, and a short post-milestone watch list; `docs/strategies/15.3_METADATA_SEARCH_SIGNAL_HYGIENE.md` is now explicitly marked not triggered; `docs/milestones/15_CRYPTOZING_APP_SEO_BOOTSTRAP.md` now rolls the milestone up as complete; and `docs/PLAN.md` now advances the active milestone to MS16.

2026-03-27 | MS15 no longer keeps a standalone strategy doc for the untriggered Phase 3
`docs/strategies/15.3_METADATA_SEARCH_SIGNAL_HYGIENE.md` has been deleted, `docs/milestones/15_CRYPTOZING_APP_SEO_BOOTSTRAP.md` now records that Phase 3 was not triggered and therefore did not keep its own strategy doc, and `README.md` no longer points at a never-used MS15 Phase 3 strategy file.

2026-03-27 | MS15 milestone exit criteria are now explicitly checked off
`docs/milestones/15_CRYPTOZING_APP_SEO_BOOTSTRAP.md` now marks each exit criterion as satisfied instead of leaving the milestone closeout only implied by the phase rollup and completion notes.

2026-03-27 | MS15 phase rollup now makes the conditional Phase 3 scope explicit in-line
`docs/milestones/15_CRYPTOZING_APP_SEO_BOOTSTRAP.md` now adds a short parenthetical on the Phase 3 rollup line to make it clear that the metadata/search-signal phase only would have applied if the existing placeholder content had been deemed too thin.

2026-03-27 | Notifications spec no longer locks MS16 to the current delivery plumbing
`docs/specs/NOTIFICATIONS.md` no longer treats preserving the existing queued/SMTP/Mailgun-aliasing shape as a rigid goal, leaving MS16 free to evaluate safer delivery safeguards and transport choices without fighting the canonical notifications scope.

2026-03-27 | MS16 now has a milestone stub for mailer safety, provider recovery, and transport decisions
`docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md` now exists as the active MS16 execution doc, `docs/PLAN.md` now points at it as the primary next doc, and `README.md` now lists it in the documentation map; the stub captures the four immediate MS16 fronts (spam-bug triage, app-side safeguards, Mailgun recovery, and SMTP-vs-HTTP-API evaluation) plus the open question about whether some payment-triggered mail needs broader owner confirmation before send.

2026-03-27 | Notifications spec no longer mislabels the public invoice URL as signed
`docs/specs/NOTIFICATIONS.md` now refers to the invoice email including a public link rather than a signed public link, matching the actual tokenized public-share implementation instead of implying cryptographic signing.

2026-03-27 | Notifications spec now uses consistent numbering through its checklist-style sections
`docs/specs/NOTIFICATIONS.md` now extends the user's numbered-section cleanup through the remaining goals, flows, alert triggers, implementation requirements, mailables, copy, testing, open-questions, and future-enhancement lists so the whole spec uses one numbering style instead of mixing bullets and numbered items.

2026-03-27 | MS16 now preserves the candidate low-information payment-acknowledgment copy and follow-through requirement
`docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md` now records the current candidate client-facing acknowledgment copy for a detected Bitcoin payment plus the matching MS16 reminder that the operator side must actually notify the invoice issuer promptly and surface a visible review/receipt CTA if that acknowledgment path is adopted.

2026-03-27 | MS16 payment-acknowledgment draft copy is now less self-undermining
`docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md` now trims the candidate client acknowledgment from “was detected and is being reviewed” down to “was detected” so the notification reassures without sounding like it is backpedaling the same claim in its next line.

2026-03-27 | Notifications Goal 2 now matches the emerging MS16 acknowledgment-vs-receipt split
`docs/specs/NOTIFICATIONS.md` no longer promises an automatic watcher-driven paid receipt; Goal 2 now says the system should enable automatic, non-promissory payment acknowledgments and truthful receipt emails, leaving room for owner-reviewed receipts and the narrower client acknowledgment idea recorded in MS16.

2026-03-27 | Notifications Goal 3 now covers only the non-receipt alert family
`docs/specs/NOTIFICATIONS.md` no longer uses Goal 3 as a generic overlapping “owners and clients get key alerts” statement; it now explicitly covers truthful alert emails for past-due, overpayment, underpayment, and partial-payment events so Goal 2 can keep the payment acknowledgment/receipt lane.

2026-03-27 | Notifications Goals 3 and 4 are now one clean non-receipt alert goal
`docs/specs/NOTIFICATIONS.md` now folds the useful trigger language from the old Goal 4 into Goal 3, deletes the redundant standalone Goal 4, and renumbers the remaining ambiguity/truthfulness goal so the goals section no longer repeats the same alert scope in two adjacent items.

2026-03-27 | Notifications ambiguity goal now states the cross-cutting truthfulness rule directly
`docs/specs/NOTIFICATIONS.md` no longer phrases the final goal as one narrow later-payment scenario; it now says payment-triggered mail must not overstate certainty when the underlying payment state is semantically ambiguous, which better matches the broader MS14/MS16 correction and acknowledgment concerns.

2026-03-27 | Notifications ambiguity goal is now less suppression-coded
`docs/specs/NOTIFICATIONS.md` now says to keep payment-triggered mail truthful when the underlying payment state is semantically ambiguous, which preserves the truthfulness goal without implying that ambiguity should automatically suppress all communication.

2026-03-27 | Notifications ambiguity goal now explicitly leaves room for safe acknowledgment
`docs/specs/NOTIFICATIONS.md` now ends the ambiguity goal with “while still acknowledging what it safely can,” closing the lingering “truth via silence” loophole without prematurely dropping down into flow-level rules.

2026-03-27 | Notifications spec now treats manual send as the action that issues a draft invoice
`docs/specs/NOTIFICATIONS.md` now states that the manual send flow is available when the invoice has a client email and its public link is enabled, and that sending the invoice queues outbound delivery, records the attempt in the delivery log, and issues the invoice out of `draft`.

2026-03-27 | Notifications flow 3.2 now separates detected-payment acknowledgment from receipt follow-up
`docs/specs/NOTIFICATIONS.md` no longer treats the second base communication flow as a simple automatic paid receipt; section 3.2 now distinguishes a low-information detected-payment acknowledgment from the higher-certainty receipt follow-up and adds owner-facing manual-follow-up expectations when no client email exists.

2026-03-27 | Notifications flow 3.2 no longer carries a dead missing-client-email fallback
`docs/specs/NOTIFICATIONS.md` no longer says receipt follow-up should fall back to manual sharing when the client email is missing, because client email is now required both by the client form validation and by the non-null schema constraint.

2026-03-27 | Notifications flow 3.2 now resolves ambiguity by narrowing the acknowledgment instead of suppressing it
`docs/specs/NOTIFICATIONS.md` no longer says an ambiguous detected payment should hold the client-facing acknowledgment; `3.2.1.4` now says the acknowledgment should stay limited to what the system can safely say and avoid certainty about how the payment applies.

2026-03-27 | Notifications flow 3.3 now stays at the product-behavior level
`docs/specs/NOTIFICATIONS.md` no longer hardcodes the `invoice_deliveries` table name, per-row field list, or the invoice detail page as the primary operator view inside the base communication flows; `3.3` now describes only the enduring shared delivery-history behavior and the operator outcomes it must expose.

2026-03-27 | Notifications Section 5 now preserves the delivery-history implementation details that no longer belong in Section 3
`docs/specs/NOTIFICATIONS.md` now carries the `invoice_deliveries` table name, delivery-row field expectations, current invoice-detail operator view note, and the full queued/sent/skipped/failed outcome set under Shared Implementation Requirements instead of letting those implementation-shaped details disappear when Section 3 was cleaned up.

2026-03-27 | Notifications alert 4.1 now stays at the product-behavior level
`docs/specs/NOTIFICATIONS.md` no longer describes the owner paid notice in terms of the `InvoicePaid` event, watcher/manual-adjustment trigger paths, or raw delivery types; `4.1` now simply states that when an invoice becomes paid, the invoice issuer gets a succinct owner notice that stays distinct from client-facing payment acknowledgments and receipts.

2026-03-27 | Notifications alert 4.2 now keeps only the past-due behavior while MS16 holds the cadence/storage details
`docs/specs/NOTIFICATIONS.md` no longer hardcodes nightly scheduling, 48-hour resend spacing, or raw delivery-log storage detail inside the Past Due alert; `docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md` now preserves those as MS16 Phase 3 decisions to flesh out later.

2026-03-27 | Notifications Section 4 now uses content requirements instead of verbatim alert copy
`docs/specs/NOTIFICATIONS.md` no longer hardcodes owner/client alert sentences inside the Past Due, Overpayment, Underpayment, and Partial-Payment sections; those lines now describe what each alert must communicate while leaving the exact wording to the later copy-guidelines and implementation work.

2026-03-27 | Notifications over/underpayment alerts now read as significant RC thresholds with neutral underpayment tone
`docs/specs/NOTIFICATIONS.md` now names Sections 4.3 and 4.4 as significant overpayment/underpayment alerts, phrases the 15% trigger as an RC threshold instead of a method call, and explicitly requires the underpayment alert to communicate the remaining balance neutrally rather than implying the client necessarily made a mistake.

2026-03-27 | Notifications spec no longer treats repeated partial payments as a separate client alert family
`docs/specs/NOTIFICATIONS.md` now removes the standalone partial-payment warning from Section 4 and folds any remaining-balance convenience guidance into the significant underpayment alert; `docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md` preserves the idea that repeated partials may still matter later as an owner-side or operational signal rather than as a distinct client-facing alert class.

2026-03-27 | Notifications spec now defines issuer copies of client-facing notifications as a general behavior, with the MS16 toggle captured separately
`docs/specs/NOTIFICATIONS.md` now says client-facing notification emails should also copy the invoice issuer by default, with issuer-level control over that behavior; the old per-alert owner-copy special casing has been removed from Section 4, and `docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md` now explicitly captures the concrete Phase 3 toggle implementation with a default-on RC posture.

2026-03-27 | Notifications Section 5 is temporarily unnamed while its contents are being recategorized
`docs/specs/NOTIFICATIONS.md` now uses `## 5. ____` as a placeholder heading so we can clean the mixed-role contents first and choose an honest section name afterward instead of locking in another misleading “implementation requirements” bucket.

2026-03-27 | Notifications item 5.1 now keeps the audit/queue invariant without hardcoding the current plumbing names
`docs/specs/NOTIFICATIONS.md` now says outbound invoice communication should use a shared queued delivery path and shared delivery history so outcomes remain auditable, replacing the older wording that hardcoded the existing queue/mailer/table implementation details.

2026-03-27 | Notifications item 5.2 now expresses delivery-history context as a product requirement instead of a row schema checklist
`docs/specs/NOTIFICATIONS.md` now says the shared delivery history must preserve enough context to identify the invoice, issuer, recipients, communication class, outcome, and timing/error details for each outbound attempt, instead of spelling that out as delivery-log row fields.

2026-03-27 | Notifications item 5.3 no longer freezes the delivery-history UI to today’s owner invoice page
`docs/specs/NOTIFICATIONS.md` no longer says the owner invoice detail page is the primary operator view for delivery history, because that is current UI placement rather than an enduring notification invariant and future versions may expose delivery history elsewhere too.

2026-03-27 | Notifications item 5.3 now defines the public-host rule without hardcoding a specific domain or runtime host
`docs/specs/NOTIFICATIONS.md` no longer ties outbound email links to `APP_PUBLIC_URL`, the currently running host, or a single production domain; it now says public-share links must use the explicitly configured public host for the intended recipient-facing environment, including cases where a dev/staging environment deliberately targets another deployment.

2026-03-27 | Notifications spec no longer carries temporary aliasing config, but MS16 preserves it for later strategy work
`docs/specs/NOTIFICATIONS.md` no longer treats catch-all aliasing as a product invariant; `docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md` now preserves the operational question for later phase planning so MS16 can decide when aliasing is still useful for safe testing, when it distorts delivery validation, and when it must be turned off before real-recipient traffic.

2026-03-27 | Notifications spec no longer treats automatic paid receipts as a settled cross-cutting rule
`docs/specs/NOTIFICATIONS.md` no longer says automatic paid receipts remain part of the owner communication model; that receipt-model decision is still being reconsidered in MS16, so it no longer belongs as a stable item in Section 5.

2026-03-27 | Notifications item 5.5 now treats outbound mail as a required deployment capability rather than a global feature-flag escape hatch
`docs/specs/NOTIFICATIONS.md` no longer says a global feature flag may disable outbound communication entirely when mail is not configured; it now says outbound mail is required for any valid recipient-facing deployment, while still allowing development and test environments to run without it intentionally.

2026-03-27 | Notifications item 5.6 now states the delivery-history outcome requirement without job/plumbing wording
`docs/specs/NOTIFICATIONS.md` now says the delivery history should surface queued, sent, skipped, and failed outcomes, replacing the more implementation-shaped wording about delivery jobs surfacing those states through a shared log.

2026-03-27 | Notifications item 5.8 no longer keeps milestone-history wording inside the spec
`docs/specs/NOTIFICATIONS.md` no longer says the later-payment owner-validation gate is “planned MS16 work, not an MS14 Phase 5 reattribution gate”; that sequencing note now lives in `docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md`, where the milestone history and follow-up planning belong.

2026-03-27 | Notifications item 5.7 now states the later-payment ambiguity warning more readably
`docs/specs/NOTIFICATIONS.md` now splits the later-payment ambiguity warning into a shorter main statement plus an examples sub-line, keeping the important “supported wallet config does not eliminate semantic ambiguity” idea without the old overlong sentence shape.

2026-03-27 | Notifications Goal 2 now keeps “automatic” scoped to acknowledgments instead of dropping it entirely
`docs/specs/NOTIFICATIONS.md` again says the system should enable automatic, non-promissory payment acknowledgments and truthful receipt emails; this preserves the automatic acknowledgment idea without forcing the spec to treat receipt emails themselves as fully automatic.

2026-03-27 | Product spec header date now reflects the current synced doc set
`docs/PRODUCT_SPEC.md` now updates its `_Last updated` header to `2026-03-27` after the MS15 closeout and MS16 kickoff docs moved forward elsewhere, without changing the underlying product-level requirements.

2026-03-27 | Notifications spec no longer treats the later-payment validation gate as a settled cross-cutting rule
`docs/specs/NOTIFICATIONS.md` now stops after the later-payment ambiguity warning instead of hardcoding the owner-validation gate itself, and its testing section now expects only the durable truthfulness behavior for semantically ambiguous payment-triggered communication; `docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md` preserves the real MS16 design question about whether such a gate is needed at all, how it coexists with safe low-information acknowledgments, which outbound classes it would cover, and whether manual sends/past-due reminders remain outside it.

2026-03-27 | Notifications spec item 5.8 now states the delivery-label invariant instead of milestone work
`docs/specs/NOTIFICATIONS.md` no longer says `MS16 delivery-log polish` in Section 5; it now simply requires the delivery history to use concise, human-friendly labels for communication classes and outcomes.

2026-03-27 | Notifications spec now keeps outbound-mail/history rules together and moves payment ambiguity into the payment flow
`docs/specs/NOTIFICATIONS.md` now renames Section 5 to `Outbound Mail and History`, moves the later-payment ambiguity note into Section 3.2 where payment acknowledgments and receipts are defined, and leaves Section 5 focused on outbound-mail and delivery-history invariants.

2026-03-27 | Notifications spec now explicitly keeps later-payment ambiguity from suppressing safe acknowledgments
`docs/specs/NOTIFICATIONS.md` now adds `3.2.2.3`, which says later-payment ambiguity should narrow the acknowledgment to what the system can safely say rather than suppressing it outright.

2026-03-27 | Notifications implementation sketches moved out of the spec and into MS16 as non-binding leftovers
`docs/specs/NOTIFICATIONS.md` Section 6 no longer hardcodes mailable classes, routes, jobs, scheduler ideas, or persistence tactics as spec requirements; those implementation-shaped notes now live in `docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md` under a clearly non-binding `Leftover Implementation Brain-Dump` section for later phase planning.

2026-03-27 | Notifications spec no longer carries an empty deferred implementation section
`docs/specs/NOTIFICATIONS.md` now deletes the former Section 6 deferral note entirely and renumbers the remaining copy, testing, open-questions, and future-enhancements sections.

2026-03-27 | Notifications spec now keeps copy guidance as a single outbound-mail principle instead of a whole section
`docs/specs/NOTIFICATIONS.md` now deletes the standalone `Copy Guidelines` section, folds the durable rule into Section 5 as `Outbound mail copy should stay concise and actionable`, and renumbers the testing/open-questions/future-enhancements sections.

2026-03-27 | Notifications spec no longer carries a standalone testing section
`docs/specs/NOTIFICATIONS.md` now deletes the former `Testing` section entirely and renumbers the remaining `Open Questions` and `Future Enhancements` sections, since the current test list was not something we would add fresh at this stage of the spec cleanup.

2026-03-27 | Notifications spec no longer carries a stale open question or post-RC future section
`docs/specs/NOTIFICATIONS.md` now ends after the outbound-mail/history rules; the stale retry-row question is deleted because the app already follows that direction, and the out-of-RC items move into `docs/BACKLOG.md` as `Notification Preference Expansion` plus the existing notification-hub backlog work.

2026-03-27 | MS16 milestone doc now records its own pending restructure task
`docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md` now includes a temporary doc-restructure task near the top: reorganize the milestone doc back to milestone-level summary, break Phase 1 through Phase 3 into their own execution docs, and eliminate the current `Open Scope Questions` and `Leftover Implementation Brain-Dump` sections by resolving or relocating their contents.

2026-03-27 | MS16 current objective now owns the doc-restructure task directly
`docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md` no longer carries a separate temporary restructure section; the work now lives where it belongs, in `Current objective`, so the milestone doc keeps one obvious top-level statement of what we are doing now.

2026-03-27 | MS16 current objective no longer pretends the notifications spec cleanup is still in progress
`docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md` now tightens `Current objective` to the actual next work: break out the phase docs and remove the milestone doc's temporary open-question / brain-dump sprawl before broader MS16 implementation proceeds.

2026-03-27 | MS16 current focus now reflects the pre-phase restructure step explicitly
`docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md` no longer says the active focus is `Phase 1`; it now says `Milestone doc restructure + phase breakout` and restores the fuller restructure intent inside `Current objective` so the milestone doc matches the actual pre-phase work in progress.

2026-03-27 | MS16 now explicitly treats its current phase count as provisional
`docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md` now says the current three-phase breakdown is provisional and should be re-cut during the phase-doc breakout if the real dependency chain points to a different shape.

2026-03-27 | MS16 current focus now defines the breakout rule directly
`docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md` now expands `Current Focus` to say the phase-doc breakout must be derived from the real work, dependency chain, and exit criteria, not from the current placeholder phase count or spec section shape, while still treating the new phase docs as sequential once they exist.

2026-03-27 | MS16 phase breakout re-cut the milestone into strategy-owned execution docs
Added `docs/strategies/16.1_DELIVERY_BASELINE_AUDIT.md`, `docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md`, `docs/strategies/16.3_PAYMENT_COMMUNICATION_TRUTHFULNESS.md`, and `docs/strategies/16.4_TEMPLATE_POLISH_RC_READINESS.md`; trimmed `docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md` back to milestone-level summary with the new four-phase rollup; and updated `docs/PLAN.md` so the active next doc is now the Phase 1 baseline-audit strategy instead of the pre-breakout milestone stub.

2026-03-27 | MS16 now locks Mailgun HTTP API as the intended transport direction
`docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md`, `docs/strategies/16.1_DELIVERY_BASELINE_AUDIT.md`, and `docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md` no longer frame transport as an open `SMTP` versus `Mailgun HTTP API` choice; MS16 now treats Mailgun HTTP API as the intended path unless a concrete blocking constraint forces an explicitly documented fallback.

2026-03-27 | MS16 strategy docs now fold constraints into tasks and inline sidecars near the work they depend on
`docs/strategies/16.1_DELIVERY_BASELINE_AUDIT.md`, `docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md`, `docs/strategies/16.3_PAYMENT_COMMUNICATION_TRUTHFULNESS.md`, and `docs/strategies/16.4_TEMPLATE_POLISH_RC_READINESS.md` no longer leave constraint-only notes as separate checklist items or bury safe parallel sidecars in end sections; those requirements and sidecars now live inline with the owning steps so the ordered path stays obvious while parallel work remains visible at the moment it becomes safe.

2026-03-27 | MS16 strategy sidecars now use numbered nested checkboxes under their owning tasks
The executable parallel work inside `docs/strategies/16.1_DELIVERY_BASELINE_AUDIT.md`, `docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md`, `docs/strategies/16.3_PAYMENT_COMMUNICATION_TRUTHFULNESS.md`, and `docs/strategies/16.4_TEMPLATE_POLISH_RC_READINESS.md` now appears as numbered nested checkbox items directly under the parent task they depend on, making them easier to reference, assign, and check off without reviving separate sidecar sections.

2026-03-27 | MS16 Phase 2 now spells out the Mailgun HTTP API implementation checklist
`docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md` no longer leaves the Mailgun HTTP API work at the level of generic prerequisites; `16.2.3.2` is now an executable nested checklist covering package install, `config/services.php`, `config/mail.php`, the `MAIL_MAILER=mailgun` default path, and the corresponding ops/env documentation updates.

2026-03-27 | MS16 Phase 1 delivery baseline audit completed and Phase 2 is now active
`docs/strategies/16.1_DELIVERY_BASELINE_AUDIT.md` now records the completed outbound delivery inventory, current guardrail matrix, concrete spam-risk findings, and the Phase 2 / Phase 3 inputs drawn from the live code; `docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md`, `docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md`, and `docs/PLAN.md` now advance MS16 to Phase 2 as the active next execution step.

2026-03-27 | MS16 Phase 2 starts shared delivery gating and Mailgun driver wiring
Added `App\\Services\\InvoiceDeliveryService` so manual sends, receipts, owner paid notices, and alert deliveries now queue through one shared service; manual invoice sends now respect a configurable cooldown, outbound mail can be disabled via config, queued sends revalidate public-link and recipient state before delivery, queued overpayment alerts now skip when no longer truthful, Laravel mail config now includes the Mailgun HTTP driver path, `.env.example` now documents the Mailgun + mail-safety env vars, and the get-live docs now describe Mailgun API credentials instead of SMTP-first setup.

2026-03-27 | MS16 Phase 2 codifies delivery safeguards in the notifications spec and marks the implemented checklist state
`docs/specs/NOTIFICATIONS.md` now explicitly requires duplicate/too-recent send suppression, an outbound-mail circuit breaker, and queued-delivery truth revalidation before send; `docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md` now marks the shared safeguard and Laravel Mailgun wiring work complete while leaving provider-state confirmation and controlled Mailgun send verification open.

2026-03-27 | MS16 Phase 2 expands the remaining provider and verification work into operator-ready steps
`docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md` no longer leaves the open Phase 2 work as vague provider-recovery and verification bullets; the remaining Mailgun/account-state confirmation, aliasing decision, controlled live-send proof, cooldown/circuit-breaker checks, and phase-close evidence are now broken into concrete nested actions, and the already-complete phase-close items are checked off.

2026-03-27 | MS16 Phase 2 records the current local Mailgun/app-env inventory
`docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md` now checks off `16.2.1.1` with the current app-side mail state: the local environment is still on `MAIL_MAILER=smtp`, `MAILGUN_ENDPOINT=api.mailgun.net`, aliasing remains enabled to `mailer.cryptozing.app`, and the Mailgun domain/secret are not configured yet, which means the shipped Mailgun HTTP path cannot be verified from this workspace until the env is updated.

2026-03-28 | MS16 now blocks real Mailgun verification behind a two-layer idempotency guard
`docs/specs/NOTIFICATIONS.md` now explicitly requires idempotency at both delivery-intent creation and send execution, and it says that payment-scoped notices should dedupe on stable payment identity such as `txid` rather than rendered timestamps or body drift; `docs/PRODUCT_SPEC.md` now reflects intent-level and send-boundary idempotency as part of mailer hardening; and `docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md` now updates the current Mailgun env state, records the completed parts of the provider dashboard pass, adds `16.2.2.5` for the stronger idempotency hardening, and explicitly blocks any controlled live Mailgun send until that guard is in place.

2026-03-28 | PRODUCT_SPEC is pulled back to summary-level mailer requirements
`docs/PRODUCT_SPEC.md` no longer carries the implementation-shaped `intent-level` and `send-boundary` idempotency wording; it now stays at the higher-level requirement that outbound mail prevent duplicate sends and points to [`docs/specs/NOTIFICATIONS.md`](specs/NOTIFICATIONS.md) for the detailed notifications-specific idempotency rules.

2026-03-28 | MS16 Phase 2 adds queue-intent idempotency before live Mailgun verification
`App\\Services\\InvoiceDeliveryService` now uses a stable delivery-intent key plus a shared lock so concurrent duplicate queue attempts cannot create multiple real outbound intents for the same invoice/type/recipient, duplicate locked-intent attempts are recorded as `skipped`, `tests/Feature/InvoiceDeliveryTest.php` now covers that locked-intent path, and `docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md` now checks off `16.2.2.5.1` while leaving the send-boundary half of the idempotency work open.

2026-03-28 | MS16 Phase 2 allows a temporary alias-off proof window on user-controlled dev addresses
`docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md` now records that, as of `2026-03-28`, aliasing may be turned off temporarily for controlled Phase 2 verification sends because the dev-database recipient addresses are user-controlled; the exception is explicitly limited to this phase’s proof sends and does not change the later RC alias-off rollout work.

2026-03-28 | MS16 Phase 2 restores sequential ordering around the aliasing decision
`docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md` no longer leaves the future-only aliasing flip/rollback task up in the earlier aliasing decision block; that action now lives with the later verification-prep step where it actually becomes actionable, so the strategy stays sequential outside explicit parallel items.

2026-03-28 | MS16 Phase 2 removes constraint-only checkboxes from later verification gates
`docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md` no longer renders the verification gate and exit-criteria closure conditions as fake actionable checkboxes; those constraints now sit as plain notes under the relevant steps so the ordered checklist stays focused on real executable work.

2026-03-28 | MS16 Phase 2 moves the rollout-doc update to where the evidence actually exists
`docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md` no longer leaves the RC rollout-note update up in the early aliasing decision block; that doc update now sits in the later evidence-capture part of verification, where provider and aliasing findings are actually settled enough to document.

2026-03-28 | MS16 Phase 2 keeps provider-state steps sequential around endpoint proof
`docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md` no longer points the early provider-state checklist item at later live-send proof without saying what to do in sequence; the dashboard step now records the endpoint match as either settled or still pending live verification, and the later verification step now explicitly closes that pending proof.

2026-03-28 | MS16 Phase 2 adds send-boundary idempotency before live Mailgun proof
`App\\Jobs\\DeliverInvoiceMail` now claims the provider boundary by transitioning queued deliveries to `sending` under a per-intent lock, skips duplicate queued rows when a matching send is already in progress or already sent, and avoids resending deliveries that were already claimed; `App\\Services\\InvoiceDeliveryService` now reuses the same stable intent-key builder at queue and send boundaries and matches recipients case-insensitively; `tests/Feature/InvoiceDeliveryTest.php` now covers claimed `sending` rows, duplicate queued rows, and recipient-case normalization; and `docs/specs/NOTIFICATIONS.md` plus `docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md` now reflect the shipped send-boundary guard and transient `sending` state.

2026-03-28 | MS16 Phase 2 closes the real provider proof after removing the nested mailable queue
All invoice mailables under `app/Mail/` no longer implement `ShouldQueue`, so `App\\Jobs\\DeliverInvoiceMail` is now the only queue boundary and the real provider-send boundary; `tests/Feature/InvoiceDeliveryTest.php` now requires the delivery job to send mail directly instead of queueing another mailable job; controlled alias-off proof mail on invoice `MS16-PROOF-APP-20260328` now shows `accepted` and `delivered` Mailgun events plus confirmed inbox receipt; and `docs/strategies/16.2_SAFEGUARDS_PROVIDER_RECOVERY.md`, `docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md`, `docs/PLAN.md`, and `docs/ops/RC_ROLLOUT_CHECKLIST.md` now record the completed Phase 2 recovery and the Phase 3 handoff.

2026-03-28 | MS16 Phase 3 locks the initial payment-truthfulness decisions
`docs/strategies/16.3_PAYMENT_COMMUNICATION_TRUTHFULNESS.md` and `docs/specs/NOTIFICATIONS.md` now lock the initial Phase 3 direction: automatic low-information payment acknowledgments are in, ambiguity stays a narrow gate that blocks automatic reviewed receipts / paid confirmations but not narrow acknowledgments or manual sends, repeated partial-payment warnings are retired into underpayment behavior, and RC client-facing payment notifications use explicit paired owner/client delivery classes instead of an issuer-copy toggle.

2026-03-28 | MS16 Phase 3 now carries an explicit Browser QA section
`docs/strategies/16.3_PAYMENT_COMMUNICATION_TRUTHFULNESS.md` now follows the established MS14/MS15 pattern with a dedicated agent-owned Browser QA prep section plus a separate `Guided User` browser-pass section, so later implementation work has an explicit human-verification target for acknowledgments, ambiguity-gated receipts, paired owner/client notice rows, and the retirement of repeated partial-payment warnings.

2026-03-28 | MS16 Phase 3 folds Browser QA prep and run under phase verification
`docs/strategies/16.3_PAYMENT_COMMUNICATION_TRUTHFULNESS.md` no longer splits Browser QA prep, Browser QA execution, and phase verification across three top-level sections; the former sections 5 and 6 now sit as ordered verification items under the Phase 3 verification section so the strategy stays structurally tighter.

2026-03-28 | MS16 Phase 3 locks delivery-history labels to the notice model
`App\\Models\\InvoiceDelivery` now exposes human-friendly notice and outcome labels, the invoice delivery log in `resources/views/invoices/show.blade.php` now shows `Notice` plus those labels instead of raw storage keys like `owner_underpay_alert`, `tests/Feature/InvoicePaymentDisplayTest.php` now proves the user-facing wording, and `docs/specs/NOTIFICATIONS.md` plus `docs/strategies/16.3_PAYMENT_COMMUNICATION_TRUTHFULNESS.md` now record the approved delivery-history label map for the current and near-term Phase 3 notice classes.

2026-03-28 | MS16 Phase 3 adds the first owner-facing reviewed-receipt send surfaces
Paid invoices now expose a manual client-receipt send path through `App\\Http\\Controllers\\InvoiceDeliveryController`, `resources/views/invoices/show.blade.php` now surfaces that review/send control directly in payment history, `App\\Services\\DashboardSnapshot` plus `resources/views/dashboard.blade.php` now add a `Review receipt` dashboard prompt when a paid invoice still lacks a queued/sent receipt, `resources/views/settings/notifications.blade.php` now makes the automatic-receipt copy honest about later manual review/send, and `docs/specs/NOTIFICATIONS.md` plus `docs/strategies/16.3_PAYMENT_COMMUNICATION_TRUTHFULNESS.md` now record that first Phase 3 owner-facing receipt-review slice.

2026-03-28 | MS16 Phase 3 adds the first automatic reviewed-receipt ambiguity hold
`App\\Models\\Invoice` now computes automatic reviewed-receipt hold reasons from multiple active on-chain payments plus ignore/reattribution state, `App\\Listeners\\SendInvoiceReceipt` now always queues the owner paid notice while holding ambiguous automatic client receipts as skipped-for-review instead of sending them, `resources/views/invoices/show.blade.php` now shows those hold reasons in the paid-invoice receipt panel, `resources/views/settings/notifications.blade.php` now names the later-payment and correction-state hold conditions honestly, and `docs/specs/NOTIFICATIONS.md` plus `docs/strategies/16.3_PAYMENT_COMMUNICATION_TRUTHFULNESS.md` now record the shipped first-pass ambiguity gate.

2026-03-28 | MS16 Phase 3 ships txid-scoped payment acknowledgments and retires live partial-warning generation
`invoice_deliveries` now carries payment-specific context so the shared delivery intent/send-boundary guards can dedupe by detected payment identity such as `txid`; `App\\Services\\InvoicePaymentSyncService` plus `App\\Services\\InvoiceAlertService` now queue paired owner/client low-information payment acknowledgments for new detected payments before any later reviewed receipt; `App\\Jobs\\DeliverInvoiceMail` now renders and truth-revalidates those payment acknowledgment rows against the active source payment before send; the watcher no longer generates new `client_partial_warning` / `owner_partial_warning` rows as a separate live alert family; `resources/views/invoices/show.blade.php` and `resources/views/settings/notifications.blade.php` now use copy that reflects the shipped acknowledgment-versus-receipt split; `docs/specs/NOTIFICATIONS.md`, `docs/strategies/16.3_PAYMENT_COMMUNICATION_TRUTHFULNESS.md`, `docs/milestones/16_MAILER_AND_ALERTS_POLISH_AUDIT.md`, and `docs/PLAN.md` now move Phase 3 into verification prep; and focused Sail tests now cover txid-scoped acknowledgment dedupe, acknowledgment-before-receipt ordering, and the retirement of live repeated partial warnings.
